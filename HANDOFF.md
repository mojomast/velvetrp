# Handoff
## Completed: M1.6 checks, powers, and deterministic effects
## Next Task: M1.7 encounters and turn-based combat
## Context: Schema v26r1 adds repository/shared-contract support for server-derived ability, skill, save, attack, and opposed checks; exact known ability/spell power use with resource, slot, and charge costs; and closed typed active effects with concentration replacement and duration lifecycle. Checks accept intent only: terms, modifiers, totals, targets, outcomes, and timestamps are server-derived. M1.6 remains factory-only with no new HTTP route/client UI; the trusted-local boundary remains 21 operations. The canonical v26 digest is `7e3fe64f425173022d119f156f60eb36b26af2c97f29d40975f5579caa660f6a`.
## Verification: `npm run typecheck` passed. `npm run build` passed with 148 Vite modules. `npm test` passed contracts 179/179, server 1,808 plus 1 skipped, and client 232/232: 2,219 passed plus 1 skipped across 128 files. `npm run test:e2e` passed the deterministic Playwright workflow. `git diff --check` passed. Live-provider E2E was not run.
## Files Modified: M1.6 touched `packages/contracts/src/{checks.ts,powers.ts,effects.ts,index.ts}`; `packages/contracts/test/{checks.test.ts,powers.test.ts,effects.test.ts}`; `server/src/repo/{checkRepo.ts,powerRepo.ts,effectRepo.ts,campaignRepo.ts,db.ts,index.ts}`; `server/test/{m16-repository-behavior.test.ts,m16-repository-composition.test.ts,migration-v26.test.ts}`; migration fixtures; and this handoff.
