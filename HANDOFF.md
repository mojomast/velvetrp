# Handoff
## Completed: Explicit fixed mechanics-starter activation plus independent-review freshness/snapshot remediation
## Next Task: M1.5 resources, inventory, equipment, economy, and rest remains the next repository-only roadmap milestone and requires separate scope
## Context: Added the 14th trusted-local campaign HTTP operation, `PUT /api/rpg/v1/campaigns/:campaignId/mechanics-starter-setup`, without schema changes or generic catalog/builder/progression HTTP. Shared contracts own exact identity `velvet:mechanics-starter@1.1.0+2f9199b5696d`. Both campaign and mechanics flags precede query/path/media/body/repository work. Independent review remediation made campaign detail no-store in both client and server, requires exact setup HTTP 200 before body parsing, templates mechanics problem instances, and makes each setup classification/proof one short repository read transaction that does not span either write. Setup reconciliation, including rejected/ambiguous outcomes, invalidates cached/older detail generations; a same-campaign reopen/peer cannot be overwritten by a late pre-activation GET or regain duplicate PUT controls. The service still fixes `local-owner`, owns observed revision/idempotency, calls install/configure exactly once in order, never retries, and preserves stable conflicts without replacement. Historical M0 13-operation statements remain explicitly historical; current docs say 14.
## Verification: Focused client API/detail tests passed 112/112. Focused mechanics/original-starter/route server tests passed 101/101, including fresh activation through the real HTTP route and invalid media/body no-repository assertions. `git diff --check` passed. Full verification passed workspace typecheck/build, contracts 144/144, server 1,765 plus 1 skipped, client 232/232, and deterministic E2E. Live-provider E2E was not run.
## Files Modified: Contracts: `packages/contracts/src/{campaigns.ts,index.ts,mechanics-starter.ts}`, `packages/contracts/test/mechanics-starter-setup.test.ts`. Server: `server/src/{app.ts,http/problem.ts,content/mechanicsStarterCatalog.ts,content/mechanicsStarterSetup.ts,repo/campaignRepo.ts,routes/rpg/v1/features.ts}`, `server/test/{mechanics-starter-setup.test.ts,rpg-campaigns-route.test.ts}`. Client/E2E: `client/src/{api.ts,api.test.ts,roleplay/CampaignDetailPage.tsx,roleplay/CampaignDetailPage.test.tsx}`, `e2e/tests/app.spec.ts`. Docs/planning: `README.md`, `docs/{ROADMAP.md,api.md,repo-architecture.md,roleplay-architecture-2026.md,rpg-integration-plan.md}`, `devplan.md`, and this file. No commit or push was performed.
