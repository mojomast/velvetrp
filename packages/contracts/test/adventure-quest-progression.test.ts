import { describe,expect,it } from "vitest";
import { adventureProgressionCandidateSchema,adventureProgressionReadSchema,adventureQuestLifecycleCandidateSchema } from "../src/adventure-quest-progression.js";

describe("adventure quest and progression contracts",()=>{
  it("accepts only closed provider-safe candidate shapes",()=>{const digest="a".repeat(64),quest={candidateId:"quest-candidate",digest,action:"claim-reward",questTitle:"The Gate",reward:{label:"Bounty",kind:"Currency",amount:12,recipient:"Aster"},confirmationRequired:true},progression={candidateId:"progression-candidate",digest,className:"Lantern Warden",levelBefore:1,levelAfter:2,features:["Mending Light"],resources:[{label:"Focus",before:0,after:1}],confirmationRequired:true};expect(adventureQuestLifecycleCandidateSchema.parse(quest)).toEqual(quest);expect(adventureProgressionCandidateSchema.parse(progression)).toEqual(progression);expect(adventureQuestLifecycleCandidateSchema.safeParse({...quest,rewardId:"private"}).success).toBe(false);expect(adventureProgressionCandidateSchema.safeParse({...progression,previewToken:digest}).success).toBe(false);});
  it("keeps pending choices read-only and identity-free",()=>{const read={available:true,className:"Lantern Warden",currentLevel:1,eligibleLevel:2,mode:"xp",totalXp:300,milestoneCount:0,pendingChoices:[{level:2,label:"Level 2 ability choice",options:["Beacon Step","Lamp Bulwark"]}]};expect(adventureProgressionReadSchema.parse(read)).toEqual(read);expect(adventureProgressionReadSchema.safeParse({...read,selections:[]}).success).toBe(false);});
});
