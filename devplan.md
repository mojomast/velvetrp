# Development Plan

Current persistence is `v48r1`; canonical populated `v46/v47 -> v48` startup upgrades are supported; v45 and earlier are unsupported; the trusted-local RPG surface has 102 explicit operations; and M1-M5.1 are complete. Wave A has delivered bounded M5.2-M5.5 slices/checkpoints, not complete milestones; the v48 provider/adventure bridge implementation commit is pending. [ROADMAP.md](docs/ROADMAP.md) owns scope/status, and the [revision 2 integration plan](docs/revision-2-integration-plan.md) owns actionable detail. This file is only the compact status ledger.

## Complete

- [✅] M1.1-M1.10: core RPG persistence and deterministic mechanics
- [✅] M2.1-M2.11: trusted-local RPG HTTP surfaces
- [✅] M3.1-M3.8: RPG client workflows
- [✅] M4.1-M4.6: bounded AI-driven RPG integration through reviewed atomic campaign-content application at v42r1
- [✅] Revision 2 plan preparation: board decisions and repository research synthesized into the authoritative actionable plan
- [✅] Revision 2 independent plan-review corrections: queue placement, dependencies, E2E evidence, companion authority split, health/validation policy, and Unscheduled milestone detail reconciled across planning docs

## H0 — Complete (Wave H green)

- [✅] H0.1 rolling migration support foundation: executable supported migration coverage is canonical populated v40/v41->v42; the preserved archive is discoverable and does not claim v2-v39 support; startup preflight rejects persisted foreign-key corruption, unexpected v42 named artifacts, and cross-campaign generation-draft ancestry before marker or artifact mutation
- [✅] H0.2 six deterministic E2E repairs: four finalization call sites expected `200` versus authoritative `201` and now preserve the `201` public contract through an E2E-only authorized actor resolver; the attached unconfigured room expected legacy chat/back but routed to play under feature-only routing and now uses a configured-status gate with cancellation/error handling; storyline/quest setup expected creation but story `POST` returned `400` under the strict graph contract and now uses the current strict workflow with idempotent replay. The old supplied aggregate drifted: M2.5 passed and the current full suite includes 12 cases. Validation passed: client focused 3 files/113 tests plus client typecheck; server fixture/M1.5 2 files/17 tests plus server typecheck; `typecheck:e2e`; full deterministic E2E 12 passed; `git diff --check`. Commits: `ee7dfba fix(client): preserve authoritative campaign navigation`; `60afa5f test(e2e): align authoritative RPG workflows`
- [✅] H0.3 current documentation reconciliation: at H0.3 completion, active docs described v42r1, completed M4.1-M4.6, 95 trusted-local RPG HTTP operations versus the historical 92-operation M2.11 baseline, and supported canonical populated v40/v41->v42 upgrades; historical ledgers remain preserved and visibly distinguished from current, Planned, Unscheduled, deferred, and excluded status. Targeted stale-claim `rg`, 37 local Markdown links/anchors, and `git diff --check` passed; no code tests were run for this docs-only slice. Commits: `77ed4b0 docs: reconcile current RPG guidance`; `2a50a41 docs: qualify historical RPG baselines`; `8a9a1c5 docs: mark H0.3 complete`; `8061da1 docs: finalize H0.3 handoff`
- [✅] H0.4 canonical health gate: root `npm run health` runs typecheck, build, `npm test`, and deterministic E2E exactly once each in order. The first attempt timed out externally; the second exposed a global foreign-key preflight regression, corrected and scoped before the final recovery wrapper run passed all four phases in order with a unique `/dev/shm` `TMPDIR`. Independent release/security review found no material issues and confirmed deterministic/live credential isolation. Commits: `3e6c1a6 fix(repo): scope migration integrity preflight`; `f18a081 chore: add canonical health gate`; `7286ded docs: document canonical health gate`; `f6cf177 docs: mark H0 health baseline complete`; `31bec78 docs: finalize H0 handoff`

## M5 — Build Next

- [✅] M5.1 Complete/Delivered at v43r1: persisted session-root presence records `present`/`left` state and optional campaign location; running versus structurally historical at-stop projections; owner/GM place, move, and remove commands; two trusted-local routes; running-live detach protection with stopped/detached history preservation; persisted agent context; authoritative client reconciliation; deterministic no-row backfill; and supported canonical populated v41/v42->v43 upgrades
- [✅] M5.1 retained exclusions: no companion, autonomous NPC speech/action, inferred co-location, pathfinding, schedule, remote identity, or multiple active encounters; HTTP remains fixed trusted-local `local-owner`
- [✅] M5.1 commits: `0b08f5a docs: freeze M5.1 integration contract`; `30cb781 feat(contracts): define session NPC presence`; `06859d4 feat(repo): add v43 NPC presence schema`; `04d7eba fix(contracts): make presence retries receipt-only`; `4cd41d3 feat(repo): add authoritative NPC presence`; `02eeec8 fix(contracts): preserve opaque presence sessions`; `49d6f02 feat(api): add session NPC presence routes`; `50c0403 feat(agent): use persisted NPC presence context`; `759a72c feat(client): manage session NPC presence`; `7094642 test(e2e): cover session NPC presence`; `3c4a2b6 fix(api): reject presence GET bodies`; `b07a355 docs: document session NPC presence`; `3b3772a docs: reconcile M5.1 architecture status`; `4479950 fix(repo): mask foreign presence sessions`; docs/status commit remains pending
- [✅] M5.1 validation: focused contracts; server migration, support-window, NPC-presence repository, detach, route, agent-context, deterministic fixture, and general repository tests; focused client API, drawer, and registry tests; workspace and root typechecks; full deterministic E2E; and `git diff --check` all passed. The final focused nondisclosure regression and server typecheck passed; no totals are asserted here
- [✅] M5.2 repository and transport slice delivered at the historical v45r1 checkpoint: fixed-local-owner authoritative management GET and closed receipt-only command POST expose create companion/create grant/revoke grant, with client transport only. v45 preserves all v44 rows while moving historical references to durable principals; this is not a current support-window claim. M5.2 remains In Progress/partial with no companion UI, public member HTTP, dismissal, proposal/decision administration, or grant exercise. Commits include `42d9934`, `839f70f`, `409103e`
- [✅] M5.3 no-schema Slice 0 delivered: active encounter HP authority, atomic actor-health mirroring through M15, fail-closed external health mutation, round-wrap effects, concentration until replacement/removal, stable replay, and ambiguous no-auto-retry post-RNG/pre-commit failure; commit `68b3c57`
- [✅] M5.3 consumable flow delivered as a separate legal-action GET, command POST, and exact-result GET plus combat UI. It supports exact pinned category `consumable`, quantity one, ordered damage/healing/health-guard-focus effects, and atomic inventory/action/combat/log/receipt settlement. The UI uses exact server target/action cost, writes a durable ambiguity marker before POST, never automatically retries, and reconciles exact result plus combat/log/actions across reload. Commit `87d53ab` closes the modifier decision by making all durations contract-ineligible: instant semantics are unavailable and noninstant modifiers are unsupported, with no consumable modifier descriptor, settlement, legal action, runtime, or successful historical result. Shared catalog/power modifier contracts are unchanged and powers retain receipt-only instant behavior. The legacy combat union is unchanged; M5.3 remains partial unless its other scope is complete. Earlier commits include `ff2610c`, `e27402d`, `77505fb`, `bab31cc`
- [✅] v45 fixture correction: corruption assertions now identify the current schema marker 45 rather than marker 44; commit `cd885dc`
- [✅] M5.4 pure no-schema exact-candidate v1 contract/vector checkpoint Complete: first kind `actor.travel`, provider-safe projection, lifecycle/quote/decision/supersession/expiry/receipt bindings, and fail-closed gates; commit `a2b479c`. The historical v46 issuance/lifecycle foundation and current v47 repository generation, exact selection/execution, and immutable receipt links are delivered; provider/adventure integration is next
- [✅] M5.5 pure no-schema protocol/vector checkpoint Complete: exact versioned framing, platform SHA-256/HKDF/HMAC, rejection sampling, mandatory observed->contributed capability state, lifecycle, and proof vectors; commit `3d834f4`. Persistence and command/client integration remain Planned
- [✅] Validation: deterministic E2E passed 15 cases including companion create/grant/revoke/replay and one response-after-commit consumable POST/reload against a coherent custom immutable published pack; fixture additions remain harness-only and catalog attestation is unchanged. E2E commit: `441defc`
- [✅] Historical M5.4 v46r1 persistence-only checkpoint: stable turn-owned `adventure-turn:<turnId>` scope, exact single actor/no confirmation/no quote, fail-closed max32 and exact empty batches, server-time expiry, and exact-scope supersession. Its five persistence categories were batches, candidates, supersessions, expiration observations, and attestation. At that checkpoint generation, execution, world adapter, receipt links, provider, routes, client, and E2E were absent. Commits: `e0ef5f8`, `a690e3a`, `71393a9`; the checkpoint gate passed 38 contract tests and 83 server candidate/migration/support/repository tests plus contract and server typechecks

## Approved queue

- [✅] M5.4 travel prerequisites delivered: authoritative candidate generation and the connection-scoped world travel adapter now share one fail-closed policy; commits `bd921b8`, `c05a289`
- [✅] M5.4 v47 exact candidate execution slice: immutable attested receipt links, canonical selection/result frames and digests, deterministic world keys, atomic actor travel execution, cryptographically reconstructed replay, current-authority replay gate, and rollback; no provider/adventure/tool/routes/client/E2E exposure. Validation passed 41 exact-candidate contract tests and 126 server migration/support/candidate/world/repository tests plus contract and server typechecks; active discovery includes five v47 migration tests and three support-window tests
- [✅] M5.4 v48 provider/adventure exact travel bridge: strict provider-safe candidate tool, immutable settled provider/selection/execution accounting, crash recovery, private receipt projection, and safe travel narration; no candidate route/client API
- [✅] M5.4 receipt-only HTTP/client/E2E integration: existing command receipt GET exposes an exact player-safe travel variant, CampaignPlay renders durable travel completion across reload, and deterministic E2E covers one provider-committed move; no live candidate generation/selection route or client selector, API remains 102
- [✅] M5.4 receipt-only review remediation: lifecycle-scoped StrictMode-safe client request cache with rejection recovery, observer member parity, selected-binding-only v48 integrity verification, world travel revision labeling, and exact-once/private-state E2E evidence
- [✅] M5.4 final receipt blockers: zero-frame stale UI prevention, scoped comprehensive narration and existing/raced binding verification, and binding-independent fixture-only exact-once world evidence
- [✅] M5.4 final receipt authorization: public destinations remain visible to current members; discovered connection/destination receipts require the still-authorized exact source-turn principal; GM-only remains hidden; active docs reconciled to receipt/E2E delivery and absent live selection APIs at API102
- [✅] M5.4 signoff: narration now enforces current player-safe connection and destination visibility with exact root-principal/current-authority discovered semantics; README and current deterministic E2E total reconciled to 16
- [✅] Campaign generation/content/expansion lane: v50 foundation plus v52 sparse section candidates for outline/arcs/locations/factions/NPCs/quests/encounters/clues/story/handouts/scenes, accepted-key dependency resolution, explicit selective apply, bounded concurrent-call reconciliation and acknowledged failed-attempt retries, exact provider usage/cost provenance, standard quest projection, and durable same-campaign NPC placement reconciliation
- [✅] v51 gameplay materialization lane: atomic exact-once starter inventory/wallet settlement on finalization, recipient-visible combat reward state and explicit atomic wallet claim, optional designated-start auto-placement plus safe GM bootstrap placement, standard travel compatibility, focused contracts/repository/routes/client projection, and standalone additive migration coverage (main integration owns schema/db wiring)
- [✅] v51 client combat reward integration: recipient-visible bundles load with combat refresh, explicit responsive unclaimed/claimed settlement UI, durable one-intent claim locking, authoritative read reconciliation without POST replay, bound actor wallet refresh, and focused API/component coverage
- [✅] Deterministic generated-campaign journey integration: one real-repository/buildApp test covers sparse v52 generation and selected-key apply, standard foundation/quest/world reads, active-session character finalization with immediate starter currency and auto-placement, provider-assisted exact travel, narrated combat, completion rewards, explicit claim/wallet settlement, and restart reconciliation without an external provider
- [✅] v51 exact combat reward claim reconciliation: recipient-controller-only immutable command/receipt/settlement-backed GET by combat, bundle, and original idempotency or claim identity; strict campaign/combat/actor/bundle/request-digest binding; private command/controller nondisclosure; exact-first client recovery followed by authoritative reward/wallet refresh; committed results unlock lagging ambiguous unclaimed reads without POST replay
- [✅] v53 campaign expansion materialization: stable-ID v34 story graphs with authoritative provenance, inert encounter planning reads, explicit idempotent public-material delivery, player-safe projections, GM-only nondisclosure, campaign administration/play integration, and focused read-after-apply/privacy/idempotency/fresh-upgrade coverage
- [✅] v52 campaign generator reviewed UI: sparse section requests, bounded revision/expansion direction, complete candidate previews, dependency-aware selective apply, stable explicit retry intents with failed-attempt acknowledgement, and publication reconciliation with focused client coverage
- [✅] v53 migration preflight regression repair: marker-45 foreign-key corruption is checked read-only before schema dependency resolution; current-schema corruption remains owned by domain-specific classifiers
- [✅] Campaign-character contention test stabilization: replace scheduler-sensitive elapsed-time threshold with direct shared-state proof that the competing SQLite commit releases between invocation and successful aggregate commit
- [✅] Client generalized combat receipt repair: receipt and outcome discriminators are narrowed before variant-only reads; public combat actions/outcomes render readable labels without private IDs; dice and existing mechanic receipt coverage remains green
- [ ] Build Later: closed rules IR, licensed offline ingestion (blocked on promoted/delivered mutable authoring unless a separately approved immutable-draft-only path), ephemeral simulation, remote tenancy/authenticated sessions and exercisable grants, harness overrides, proactive automation/tools
- [ ] Approved Build Unscheduled: append-only multiclass, mutable logical unpinned pack authoring over immutable revisions, zones/range bands, boss phases, autonomous parties

Discord, VTT adapters, and simultaneous encounters remain deferred. Preserve one active encounter per campaign session.
