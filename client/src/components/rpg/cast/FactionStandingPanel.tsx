import type { CreateCampaignFactionHttpRequest, FactionReputationCommandHttpRequest, GmCampaignFactionsHttpResponse, PlayerCampaignFactionsHttpResponse } from "@velvet/contracts";
import { FormEvent, useState } from "react";

export function FactionStandingPanel({ data, revision, isGm, locked, onCreate, onReputation, onPreview }: {
  data: GmCampaignFactionsHttpResponse | PlayerCampaignFactionsHttpResponse; revision: number; isGm: boolean; locked: boolean;
  onCreate: (input: CreateCampaignFactionHttpRequest) => Promise<void>; onReputation: (factionId: string, input: FactionReputationCommandHttpRequest) => Promise<void>;
  onPreview: (input: CreateCampaignFactionHttpRequest) => void;
}) {
  const [name,setName]=useState(""),[description,setDescription]=useState(""),[notes,setNotes]=useState(""),[visibility,setVisibility]=useState<"public"|"gm">("public"),[previewRequest,setPreviewRequest]=useState<CreateCampaignFactionHttpRequest|null>(null);
  const [selected,setSelected]=useState(""),[actor,setActor]=useState(""),[delta,setDelta]=useState("1"),[reason,setReason]=useState("");
  const changed=()=>setPreviewRequest(null);
  const build=():CreateCampaignFactionHttpRequest=>({name,publicState:{description},privateState:{gmNotes:notes,visibility},expectedRevision:revision,idempotencyKey:`faction-ui-${crypto.randomUUID()}`});
  function preview(){const input=build();setPreviewRequest(input);onPreview(input);}
  function create(event:FormEvent){event.preventDefault();if(previewRequest)void onCreate(previewRequest);}
  function reputation(event:FormEvent){event.preventDefault();void onReputation(selected,{subjectActorId:actor,delta:Number(delta),reason,expectedRevision:revision,idempotencyKey:`reputation-ui-${crypto.randomUUID()}`});}
  return <section className="studio-panel" aria-labelledby="factions-heading"><h2 id="factions-heading">Factions & standings</h2><p className="field-help">Persisted faction definitions are read-only; current routes support creation and reputation commands.</p>
    {data.factions.length?<ul className="cast-list">{data.factions.map((faction)=><li key={faction.factionId}><h3><bdi dir="auto">{faction.name}</bdi></h3><p><bdi dir="auto">{faction.publicState.description}</bdi></p>{isGm&&"privateState"in faction&&<dl className="private-record"><div><dt>Visibility</dt><dd>{faction.privateState.visibility}</dd></div><div><dt>GM notes</dt><dd><bdi dir="auto">{faction.privateState.gmNotes||"None"}</bdi></dd></div></dl>}<ul aria-label={`Standings for ${faction.name}`}>{data.standings.filter((item)=>item.factionId===faction.factionId).map((standing)=><li key={`${standing.factionId}:${standing.subjectActorId}`}>Reputation {standing.reputation}</li>)}</ul></li>)}</ul>:<p>No visible factions.</p>}
    {isGm&&<><form className="studio-form" onSubmit={create}><h3>Create faction definition</h3><label>Name<input required value={name} onChange={(e)=>{setName(e.target.value);changed()}} /></label><label>Player description<textarea value={description} onChange={(e)=>{setDescription(e.target.value);changed()}} /></label><label>Private GM notes<textarea value={notes} onChange={(e)=>{setNotes(e.target.value);changed()}} /></label><label>Visibility<select value={visibility} onChange={(e)=>{setVisibility(e.target.value as "public"|"gm");changed()}}><option value="public">Public</option><option value="gm">GM only</option></select></label><div className="button-row"><button type="button" className="ghost" disabled={locked||!name.trim()} onClick={preview}>Preview exact player projection</button><button className="primary" disabled={locked||!previewRequest}>Create exact previewed definition</button></div></form>
      <form className="studio-form" onSubmit={reputation}><h3>Change reputation</h3><label>Faction<select required value={selected} onChange={(e)=>setSelected(e.target.value)}><option value="">Choose faction</option>{data.factions.map((item)=><option value={item.factionId} key={item.factionId}>{item.name}</option>)}</select></label><label>Subject actor ID<input required value={actor} onChange={(e)=>setActor(e.target.value)} /></label><label>Exact reputation delta<input required type="number" value={delta} onChange={(e)=>setDelta(e.target.value)} /></label><label>Reason<input required value={reason} onChange={(e)=>setReason(e.target.value)} /></label><button className="primary" disabled={locked||!selected||!actor||!reason||Number(delta)===0}>Apply exact reputation command</button></form></>}
  </section>;
}
