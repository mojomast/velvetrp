import { createHash } from "node:crypto";
import type DatabaseDriver from "better-sqlite3";
import { applyCharacterProgressionInputSchema, correctCharacterXpInputSchema, grantCharacterMilestoneInputSchema, grantCharacterXpInputSchema, progressionCommandResultSchema, progressionEventSchema, progressionReceiptSchema, progressionStateSchema, resourceIdSchema, utcIsoTimestampSchema, type ApplyCharacterProgressionInput, type CharacterDerivedStats, type CorrectCharacterXpInput, type GrantCharacterMilestoneInput, type GrantCharacterXpInput, type ProgressionCommandResult, type ProgressionEvent, type ProgressionPreview } from "@velvet/contracts";
import type { Clock, IdGenerator } from "../../runtime.js";
import { progressionCatalogDigest, progressionReferenceKey, type ExactReference } from "../../characterProgressionCatalog.js";
import { canonicalProgressionJson, canonicalStarterProgressionProfile, progressionProfileDigest, starterProgressionProfileId } from "../../characterProgressionProfile.js";
import { canonicalCatalogJson } from "../contentCatalog/index.js";
import { loadCanonicalProgressionProfile, loadExactProgressionCatalog, type ProgressionRootRow } from "../characterProgressionPersistence.js";
import { CharacterProgressionAuthorizationError, CharacterProgressionConflictError, CharacterProgressionStaleError, CharacterProgressionUnavailableError } from "./characterProgressionErrors.js";
import type { CharacterProgressionReadRepository } from "./characterProgressionReadRepo.js";

const digest=(value:unknown)=>createHash("sha256").update(canonicalCatalogJson(value)).digest("hex");
const sortReferences=<T extends ExactReference>(values:T[])=>[...values].sort((left,right)=>progressionReferenceKey(left).localeCompare(progressionReferenceKey(right)));

/** Installs and validates the canonical starter progression profiles. */
function ensureProfiles(db:DatabaseDriver.Database):void { if(!db.prepare("SELECT 1 FROM rpg_rules_profiles WHERE rules_profile_id='velvet:rules:starter-v1'").get())throw new CharacterProgressionUnavailableError("unsupported rules profile"); const insert=db.prepare(`INSERT OR IGNORE INTO rpg_progression_profiles_v23(profile_id,rules_profile_id,mode,max_level,thresholds_json,profile_digest) VALUES(?,'velvet:rules:starter-v1',?,3,?,?)`); for(const mode of ["xp","milestone"] as const){const profile=canonicalStarterProgressionProfile(mode);insert.run(profile.profileId,mode,canonicalProgressionJson(profile.thresholds),progressionProfileDigest(mode));loadCanonicalProgressionProfile(db,profile.profileId);} }

/** Initializes exact class/race progression records when the character has complete profile support. */
export function initializeCharacterProgressionV24(db:DatabaseDriver.Database,input:{campaignCharacterId:string;campaignId:string;sheetId:string;actorId:string;classRef:{packId:string;packVersion:string;definitionId:string};derived:CharacterDerivedStats;now:string;mode?:"xp"|"milestone"}):boolean { ensureProfiles(db);const mode=input.mode??"xp",candidate:ProgressionRootRow={campaign_character_id:input.campaignCharacterId,campaign_id:input.campaignId,sheet_id:input.sheetId,actor_id:input.actorId,profile_id:starterProgressionProfileId(mode),class_pack_id:input.classRef.packId,class_pack_version:input.classRef.packVersion,class_definition_id:input.classRef.definitionId,level:1,total_xp:0,milestone_count:0,revision:0,derived_json:canonicalCatalogJson(input.derived),created_at:input.now,updated_at:input.now};let catalog:ReturnType<typeof loadExactProgressionCatalog>;try{catalog=loadExactProgressionCatalog(db,candidate);}catch{return false;} const result=db.prepare(`INSERT OR IGNORE INTO character_progression_v23(campaign_character_id,campaign_id,sheet_id,actor_id,profile_id,class_pack_id,class_pack_version,class_kind,class_definition_id,level,total_xp,milestone_count,revision,derived_json,created_at,updated_at) VALUES(?,?,?,?,?,?,?,'class',?,1,0,0,0,?,?,?)`).run(input.campaignCharacterId,input.campaignId,input.sheetId,input.actorId,candidate.profile_id,input.classRef.packId,input.classRef.packVersion,input.classRef.definitionId,candidate.derived_json,input.now,input.now);if(result.changes===0)return false;const sheet=db.prepare("SELECT race_pack_id,race_pack_version,race_definition_id FROM rpg_campaign_sheets WHERE id=?").get(input.sheetId) as any,levelsJson=canonicalCatalogJson(catalog.levels),powersJson=canonicalCatalogJson(catalog.initialPowers);db.prepare(`INSERT INTO character_progression_bootstrap_v24(campaign_character_id,race_pack_id,race_pack_version,race_kind,race_definition_id,class_progression_json,class_progression_digest,initial_powers_json,initial_powers_digest,created_at) VALUES(?,?,?,'race',?,?,?,?,?,?)`).run(input.campaignCharacterId,sheet.race_pack_id,sheet.race_pack_version,sheet.race_definition_id,levelsJson,progressionCatalogDigest(catalog.levels),powersJson,progressionCatalogDigest(catalog.initialPowers),input.now);db.prepare("INSERT INTO character_progression_snapshots_v23(campaign_character_id,revision,command_id,snapshot_json,created_at) VALUES(?,0,NULL,?,?)").run(input.campaignCharacterId,canonicalCatalogJson({campaignCharacterId:input.campaignCharacterId,level:1,totalXp:0,milestoneCount:0,revision:0,derived:input.derived}),input.now);const power=db.prepare(`INSERT INTO character_known_powers_v23(campaign_character_id,kind,pack_id,pack_version,definition_id,source_level,source_choice_id,granted_by_command_id,granted_at) VALUES(?,?,?,?,?,1,NULL,NULL,?)`),sourceInsert=db.prepare(`INSERT INTO character_known_power_sources_v24(campaign_character_id,kind,pack_id,pack_version,definition_id,source_kind,source_reference_json,source_digest) VALUES(?,?,?,?,?,?,?,?)`);for(const source of catalog.initialPowers){const reference=source.reference,sourceJson=canonicalCatalogJson(source.sourceReference);power.run(input.campaignCharacterId,reference.kind,reference.packId,reference.packVersion,reference.definitionId,input.now);sourceInsert.run(input.campaignCharacterId,reference.kind,reference.packId,reference.packVersion,reference.definitionId,source.source,sourceJson,progressionCatalogDigest(source.sourceReference));}const pendingJson=canonicalCatalogJson([]);db.prepare(`INSERT INTO character_progression_pending_snapshots_v24(campaign_character_id,revision,command_id,pending_json,pending_digest,created_at) VALUES(?,0,NULL,?,?,?)`).run(input.campaignCharacterId,pendingJson,progressionCatalogDigest([]),input.now);return true; }

function retry(db:DatabaseDriver.Database,campaignId:string,actor:string,key:string){return db.prepare(`SELECT command.type,command.requested_json,command.campaign_character_id,receipt.result_json FROM character_progression_commands_v23 command JOIN character_progression_receipts_v23 receipt ON receipt.campaign_character_id=command.campaign_character_id AND receipt.command_id=command.command_id JOIN character_progression_command_proposals_v24 proposal ON proposal.campaign_character_id=command.campaign_character_id AND proposal.command_id=command.command_id AND proposal.proposed_result_json=receipt.result_json JOIN character_progression_events_v24 event ON event.campaign_character_id=command.campaign_character_id AND event.command_id=command.command_id AND event.event_id=proposal.proposed_event_id WHERE command.campaign_id=? AND command.actor_principal_id=? AND command.idempotency_key=?`).get(campaignId,actor,key) as any;}
function exactRetry(found:any,type:string,requested:unknown,id:string):ProgressionCommandResult|null{if(!found)return null;if(found.type!==type||found.campaign_character_id!==id||found.requested_json!==canonicalCatalogJson(requested))throw new CharacterProgressionConflictError("idempotency key was reused for a different progression command");return progressionCommandResultSchema.parse(JSON.parse(found.result_json));}
const snapshotJson=(row:ProgressionRootRow)=>canonicalCatalogJson({campaignCharacterId:row.campaign_character_id,level:row.level,totalXp:row.total_xp,milestoneCount:row.milestone_count,revision:row.revision,derived:JSON.parse(row.derived_json)});
function insertCommandProposal(db:DatabaseDriver.Database,values:{row:ProgressionRootRow;actor:string;commandId:string;event:ProgressionEvent;key:string;type:string;expected:number;requested:unknown;now:string;result:ProgressionCommandResult}){const resultJson=canonicalCatalogJson(values.result),requestedJson=canonicalCatalogJson(values.requested);db.prepare(`INSERT INTO character_progression_commands_v23(campaign_character_id,command_id,campaign_id,actor_principal_id,idempotency_key,type,expected_revision,requested_json,request_digest,proposed_result_json,created_at) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).run(values.row.campaign_character_id,values.commandId,values.row.campaign_id,values.actor,values.key,values.type,values.expected,requestedJson,digest(values.requested),resultJson,values.now);db.prepare(`INSERT INTO character_progression_command_proposals_v24(campaign_character_id,command_id,proposed_event_id,proposed_event_type,proposed_event_json,proposed_result_json) VALUES(?,?,?,?,?,?)`).run(values.row.campaign_character_id,values.commandId,values.event.eventId,values.event.type,canonicalCatalogJson(values.event),resultJson);}
function insertPending(db:DatabaseDriver.Database,row:ProgressionRootRow,commandId:string,pending:ProgressionPreview["pendingChoices"],now:string){const json=canonicalCatalogJson(pending);db.prepare(`INSERT INTO character_progression_pending_snapshots_v24(campaign_character_id,revision,command_id,pending_json,pending_digest,created_at) VALUES(?,?,?,?,?,?)`).run(row.campaign_character_id,row.revision,commandId,json,progressionCatalogDigest(pending),now);}
function finishAudit(db:DatabaseDriver.Database,row:ProgressionRootRow,event:ProgressionEvent,expected:number,now:string,result:ProgressionCommandResult){db.prepare(`INSERT INTO character_progression_events_v24(event_id,campaign_character_id,command_id,type,revision_before,revision,occurred_at,public_data) VALUES(?,?,?,?,?,?,?,?)`).run(event.eventId,row.campaign_character_id,event.commandId,event.type,expected,row.revision,now,canonicalCatalogJson(event.publicData));db.prepare("INSERT INTO character_progression_snapshots_v23(campaign_character_id,revision,command_id,snapshot_json,created_at) VALUES(?,?,?,?,?)").run(row.campaign_character_id,row.revision,event.commandId,snapshotJson(row),now);db.prepare("INSERT INTO character_progression_receipts_v23(campaign_character_id,command_id,revision_before,revision_after,result_json) VALUES(?,?,?,?,?)").run(row.campaign_character_id,event.commandId,expected,row.revision,canonicalCatalogJson(result));}
function eventFor(values:{eventId:string;commandId:string;characterId:string;type:ProgressionEvent["type"];revision:number;now:string;publicData:ProgressionEvent["publicData"]}){return progressionEventSchema.parse({eventId:values.eventId,commandId:values.commandId,campaignCharacterId:values.characterId,type:values.type,revision:values.revision,occurredAt:values.now,publicData:values.publicData});}

/** Dependencies shared by progression commands and the read factory. */
export interface CharacterProgressionWriteDependencies { clock:Clock; ids:IdGenerator; reads:CharacterProgressionReadRepository; assertFactoryMutation():void; }
/** State-changing character progression commands. */
export interface CharacterProgressionWriteRepository {
  /** Grants XP using the character's XP progression profile. */
  grantCharacterXp(actorPrincipalId:string,campaignCharacterId:string,input:GrantCharacterXpInput):ProgressionCommandResult;
  /** Grants one milestone using the character's milestone progression profile. */
  grantCharacterMilestone(actorPrincipalId:string,campaignCharacterId:string,input:GrantCharacterMilestoneInput):ProgressionCommandResult;
  /** Reverses a previously granted XP or milestone ledger entry. */
  correctCharacterProgressionEntry(actorPrincipalId:string,campaignCharacterId:string,input:CorrectCharacterXpInput):ProgressionCommandResult;
  /** Applies a validated, current advancement preview and its selections. */
  applyCharacterProgression(actorPrincipalId:string,campaignCharacterId:string,input:ApplyCharacterProgressionInput):ProgressionCommandResult;
}

/** Creates immediate-transaction progression commands with ordered provenance and audit writes. */
export function createCharacterProgressionWriteRepository(db:DatabaseDriver.Database,deps:CharacterProgressionWriteDependencies):CharacterProgressionWriteRepository { const {reads}=deps,authorized=reads.getAuthorized,rootFor=reads.rootFor,authority=reads.authority,stateFor=reads.getState,previewFor=reads.getPreview;
  const ledgerCommand=(kind:"grant-xp"|"grant-milestone",actor:string,id:string,input:GrantCharacterXpInput|GrantCharacterMilestoneInput)=>{deps.assertFactoryMutation();const normalized=(kind==="grant-xp"?grantCharacterXpInputSchema:grantCharacterMilestoneInputSchema).parse(input) as any;return db.transaction(()=>{const row=authorized(actor,id);if(!row)throw new CharacterProgressionAuthorizationError();const again=exactRetry(retry(db,row.campaign_id,actor,normalized.idempotencyKey),kind,normalized,id);if(again)return again;if(row.revision!==normalized.expectedRevision)throw new CharacterProgressionStaleError();const mode=loadCanonicalProgressionProfile(db,row.profile_id).mode;if((kind==="grant-xp")!==(mode==="xp"))throw new CharacterProgressionConflictError("grant kind does not match progression mode");const now=utcIsoTimestampSchema.parse(deps.clock.now().toISOString()),commandId=resourceIdSchema.parse(deps.ids.nextId()),eventId=resourceIdSchema.parse(deps.ids.nextId()),entryId=resourceIdSchema.parse(deps.ids.nextId());db.prepare("UPDATE character_progression_v23 SET total_xp=total_xp+?,milestone_count=milestone_count+?,revision=revision+1,updated_at=? WHERE campaign_character_id=?").run(kind==="grant-xp"?normalized.amount:0,kind==="grant-milestone"?1:0,now,id);const updated=rootFor(id)!,preview=previewFor(updated),state=stateFor(updated,preview.pendingChoices),receipt=progressionReceiptSchema.parse({commandId,campaignCharacterId:id,idempotencyKey:normalized.idempotencyKey,type:kind,revisionBefore:row.revision,revisionAfter:updated.revision,occurredAt:now,state,appliedLevels:[]}),result=progressionCommandResultSchema.parse({progression:state,receipt}),event=eventFor({eventId,commandId,characterId:id,type:"progress_granted",revision:updated.revision,now,publicData:{kind:"grant",mode,amount:kind==="grant-xp"?normalized.amount:1}});insertCommandProposal(db,{row:updated,actor,commandId,event,key:normalized.idempotencyKey,type:kind,expected:row.revision,requested:normalized,now,result});db.prepare(`INSERT INTO character_progression_ledger_v23(entry_id,campaign_character_id,command_id,kind,xp_delta,milestone_delta,correction_of_entry_id,reason,occurred_at) VALUES(?,?,?,?,?,?,NULL,?,?)`).run(entryId,id,commandId,kind==="grant-xp"?"xp":"milestone",kind==="grant-xp"?normalized.amount:0,kind==="grant-milestone"?1:0,normalized.reason,now);insertPending(db,updated,commandId,preview.pendingChoices,now);finishAudit(db,updated,event,row.revision,now,result);return result;}).immediate();};
  return { grantCharacterXp(actor,id,input){return ledgerCommand("grant-xp",actor,id,input);},grantCharacterMilestone(actor,id,input){return ledgerCommand("grant-milestone",actor,id,input);},correctCharacterProgressionEntry(actor,id,input){deps.assertFactoryMutation();const normalized=correctCharacterXpInputSchema.parse(input);return db.transaction(()=>{const row=authorized(actor,id);if(!row)throw new CharacterProgressionAuthorizationError();const role=authority(actor,row);if(role!=="owner"&&role!=="gm")throw new CharacterProgressionAuthorizationError();const again=exactRetry(retry(db,row.campaign_id,actor,normalized.idempotencyKey),"correct-xp",normalized,id);if(again)return again;if(row.revision!==normalized.expectedRevision)throw new CharacterProgressionStaleError();const original=db.prepare("SELECT * FROM character_progression_ledger_v23 WHERE entry_id=? AND campaign_character_id=? AND kind IN ('xp','milestone')").get(normalized.entryId,id) as any;if(!original)throw new CharacterProgressionConflictError("corrected entry is unavailable");const now=utcIsoTimestampSchema.parse(deps.clock.now().toISOString()),commandId=resourceIdSchema.parse(deps.ids.nextId()),eventId=resourceIdSchema.parse(deps.ids.nextId()),entryId=resourceIdSchema.parse(deps.ids.nextId());db.prepare("UPDATE character_progression_v23 SET total_xp=total_xp-?,milestone_count=milestone_count-?,revision=revision+1,updated_at=? WHERE campaign_character_id=?").run(original.xp_delta,original.milestone_delta,now,id);const updated=rootFor(id)!,preview=previewFor(updated),state=stateFor(updated,preview.pendingChoices),receipt=progressionReceiptSchema.parse({commandId,campaignCharacterId:id,idempotencyKey:normalized.idempotencyKey,type:"correct-xp",revisionBefore:row.revision,revisionAfter:updated.revision,occurredAt:now,state,appliedLevels:[]}),result=progressionCommandResultSchema.parse({progression:state,receipt}),event=eventFor({eventId,commandId,characterId:id,type:"progress_corrected",revision:updated.revision,now,publicData:{kind:"correction",correctedEntryId:original.entry_id,reason:normalized.reason}});insertCommandProposal(db,{row:updated,actor,commandId,event,key:normalized.idempotencyKey,type:"correct-xp",expected:row.revision,requested:normalized,now,result});db.prepare(`INSERT INTO character_progression_ledger_v23(entry_id,campaign_character_id,command_id,kind,xp_delta,milestone_delta,correction_of_entry_id,reason,occurred_at) VALUES(?,?,?,'correction',?,?,?,?,?)`).run(entryId,id,commandId,-original.xp_delta,-original.milestone_delta,original.entry_id,normalized.reason,now);insertPending(db,updated,commandId,preview.pendingChoices,now);finishAudit(db,updated,event,row.revision,now,result);return result;}).immediate();},applyCharacterProgression(actor,id,input){deps.assertFactoryMutation();const normalized=applyCharacterProgressionInputSchema.parse(input);return db.transaction(()=>{const row=authorized(actor,id);if(!row)throw new CharacterProgressionAuthorizationError();const again=exactRetry(retry(db,row.campaign_id,actor,normalized.idempotencyKey),"apply-levels",normalized,id);if(again)return again;if(row.revision!==normalized.previewRevision)throw new CharacterProgressionStaleError();const base=previewFor(row),selected=previewFor(row,normalized.selections);if(base.token!==normalized.previewToken)throw new CharacterProgressionStaleError();if(!base.levels.length)throw new CharacterProgressionConflictError("no eligible levels");const required=new Map(base.pendingChoices.map(choice=>[choice.choiceId,choice]));if(normalized.selections.length!==required.size||new Set(normalized.selections.map(value=>value.choiceId)).size!==required.size)throw new CharacterProgressionConflictError("every required progression choice must be selected exactly once");for(const selection of normalized.selections){const choice=required.get(selection.choiceId);if(!choice||!choice.options.some(option=>progressionReferenceKey(option)===progressionReferenceKey(selection.ability)))throw new CharacterProgressionConflictError("progression selection is not an exact offered option");}const now=utcIsoTimestampSchema.parse(deps.clock.now().toISOString()),commandId=resourceIdSchema.parse(deps.ids.nextId()),eventId=resourceIdSchema.parse(deps.ids.nextId()),final=selected.levels.at(-1)!,advancementIds=selected.levels.map(()=>resourceIdSchema.parse(deps.ids.nextId())),known=stateFor(row),newAbilities=[...known.knownAbilities,...selected.levels.flatMap(level=>[...level.fixedAbilities,...level.selectedAbilities])],newSpells=[...known.knownSpells,...selected.levels.flatMap(level=>level.spells)];if(new Set(newAbilities.map(progressionReferenceKey)).size!==newAbilities.length||new Set(newSpells.map(progressionReferenceKey)).size!==newSpells.length)throw new CharacterProgressionConflictError("advancement contains a duplicate known power");const projected=progressionStateSchema.parse({...known,level:final.level,revision:row.revision+1,pendingChoices:[],knownAbilities:sortReferences(newAbilities),knownSpells:sortReferences(newSpells),derived:final.derivedAfter,updatedAt:now}),receipt=progressionReceiptSchema.parse({commandId,campaignCharacterId:id,idempotencyKey:normalized.idempotencyKey,type:"apply-levels",revisionBefore:row.revision,revisionAfter:row.revision+1,occurredAt:now,state:projected,appliedLevels:selected.levels}),result=progressionCommandResultSchema.parse({progression:projected,receipt}),event=eventFor({eventId,commandId,characterId:id,type:"levels_applied",revision:row.revision+1,now,publicData:{kind:"advancement",levels:selected.levels.map(level=>level.level)}});insertCommandProposal(db,{row,actor,commandId,event,key:normalized.idempotencyKey,type:"apply-levels",expected:row.revision,requested:normalized,now,result});db.prepare("UPDATE rpg_character_classes SET level=? WHERE sheet_id=? AND position=0").run(final.level,row.sheet_id);for(const level of selected.levels){db.prepare("UPDATE rpg_actor_resources SET current=?,max=? WHERE actor_id=? AND name='health'").run(level.hp.currentAfter,level.hp.maxAfter,row.actor_id);for(const resource of level.resources)db.prepare(`INSERT INTO rpg_actor_resources(campaign_id,actor_id,name,current,max) VALUES(?,?,?,?,?) ON CONFLICT(actor_id,name) DO UPDATE SET current=excluded.current,max=excluded.max`).run(row.campaign_id,row.actor_id,resource.resourceId,resource.currentAfter,resource.maxAfter);}db.prepare("UPDATE character_progression_v23 SET level=?,derived_json=?,revision=revision+1,updated_at=? WHERE campaign_character_id=?").run(final.level,canonicalCatalogJson(final.derivedAfter),now,id);const power=db.prepare(`INSERT INTO character_known_powers_v23(campaign_character_id,kind,pack_id,pack_version,definition_id,source_level,source_choice_id,granted_by_command_id,granted_at) VALUES(?,?,?,?,?,?,?,?,?)`),source=db.prepare(`INSERT INTO character_known_power_sources_v24(campaign_character_id,kind,pack_id,pack_version,definition_id,source_kind,source_reference_json,source_digest) VALUES(?,?,?,?,?,?,?,?)`);for(const [index,level] of selected.levels.entries()){const advancementId=advancementIds[index]!,levelSelections=normalized.selections.filter(selection=>required.get(selection.choiceId)?.level===level.level);db.prepare("INSERT INTO character_level_advancements_v23(advancement_id,campaign_character_id,command_id,level,position,preview_token,selections_json,changes_json,applied_at) VALUES(?,?,?,?,?,?,?,?,?)").run(advancementId,id,commandId,level.level,index,normalized.previewToken,canonicalCatalogJson(levelSelections),canonicalCatalogJson(level),now);for(const reference of [...level.fixedAbilities,...level.spells]){const sourceRef={advancementId,commandId,level:level.level},json=canonicalCatalogJson(sourceRef);power.run(id,reference.kind,reference.packId,reference.packVersion,reference.definitionId,level.level,null,commandId,now);source.run(id,reference.kind,reference.packId,reference.packVersion,reference.definitionId,"advancement-fixed",json,progressionCatalogDigest(sourceRef));}for(const reference of level.selectedAbilities){const selection=levelSelections.find(value=>progressionReferenceKey(value.ability)===progressionReferenceKey(reference))!,sourceRef={advancementId,choiceId:selection.choiceId,commandId,level:level.level},json=canonicalCatalogJson(sourceRef);power.run(id,reference.kind,reference.packId,reference.packVersion,reference.definitionId,level.level,selection.choiceId,commandId,now);source.run(id,reference.kind,reference.packId,reference.packVersion,reference.definitionId,"advancement-choice",json,progressionCatalogDigest(sourceRef));}}const updated=rootFor(id)!;insertPending(db,updated,commandId,[],now);const actual=stateFor(updated);if(canonicalCatalogJson(actual)!==canonicalCatalogJson(projected))throw new Error("applied progression result does not match inserted authoritative state");finishAudit(db,updated,event,row.revision,now,result);return result;}).immediate();} };
}
