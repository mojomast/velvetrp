# Handoff
## Current: M5.1 session NPC presence Complete/Delivered at v43r1; M5.2-M5.5 read-only rechecks Complete; M5.2-M5.6 implementations Planned; M5.5 threat-model checkpoint Complete
## Next Task: Preserve parallelism: start M5.2 schema/contracts with its schema steward allocating v44, the conditional no-schema M5.3 Slices 0/1 subset in disjoint files, M5.4 pure contract work, and M5.5 exact versioned binary framing, domain separation, HKDF/HMAC inputs, rejection/timeout protocol, and vectors only with no additional schema. Use one integration owner for shared files.
## Context: H0.1-H0.4 and Wave H are complete and green. Current persistence is v43r1, canonical populated v41/v42->v43 startup upgrades are supported, and the trusted-local RPG surface has 97 explicit operations. Preserve unrelated dirty work; do not inspect or modify `server/src/repo/contentCatalogRepo.ts`, stage, commit, push, clean, reset, stash, or overwrite another owner.
## Delivered Scope: Persisted session-root presence records `present`/`left` state and optional campaign location. Running projections expose the live present cast; stopped projections are structurally historical at-stop. Owner/GM place, move, and remove commands use the session-root revision. Two trusted-local routes provide role-safe reads and commands. Detach blocks only running sessions with live-present rows and preserves stopped/detached history. Agent context and the client drawer consume persisted presence, client mutations reconcile from authoritative reads, the migration backfills no presence rows, and canonical populated v41/v42->v43 upgrades are supported.
## Retained Exclusions: No companion, autonomous NPC speech/action, inferred co-location, pathfinding, schedule, remote identity, or multiple active encounters. HTTP remains fixed trusted-local `local-owner`.
## Recheck Decisions: M5.2 is contract-frozen/ready as a companion sidecar over NPC identity and v43 presence, with companion-owned proposals/decisions; owner/GM can administer grants, but no grant is exercisable before L5. M5.3 Slices 0/1 make encounter HP authoritative while active and mirror actor health atomically; decrement round durations only at round wrap; retain concentration until replacement/removal with no damage-break rule; preserve committed exact replay; treat post-RNG pre-commit failure as ambiguous with no automatic retry; and exclude area/agent actions. Their no-schema item subset is a fixed combat `use consumable` command that consumes one unit and the acting turn's action by server policy, not catalog `actionCost`; the exact pinned item must have category `consumable`, quantity >= 1, and only damage, healing, resource, or modifier effects with `instant` duration. Condition, non-instant, persistent, and Waylamp effects/items are excluded; expanding this subset requires a fresh schema decision. M5.4 is ready for a pure no-schema generic exact-candidate protocol freeze with provider-safe projection, exhaustive kind policy, explicit supersession/expiry/quote/receipt links, and required connection-scoped adapters; travel is first after lifecycle/session semantics freeze. M5.5's threat-model decisions and primitive-selection checkpoint are Complete: platform SHA-256/HKDF/HMAC/rejection sampling, an injectable secure-byte port, explicit pending/settled/abandoned/legacy states, and withholding detectable but not preventable. Exact versioned binary framing, domain separation, HKDF/HMAC inputs, rejection/timeout protocol, and vectors remain the next no-schema pure-contract task; the cryptographic protocol is not frozen and implementation is not ready beyond that checkpoint. The rechecks delivered no implementation and allocated no schema.
## H0 History: Root `npm run health` represents exactly `npm run typecheck && npm run build && npm test && npm run test:e2e`, fail-fast and once per phase. The final recovery run used a unique `/dev/shm` `TMPDIR` and passed all four phases in order after an external timeout and correction of a global foreign-key preflight regression. Independent release/security review found no material issues and confirmed deterministic/live credential isolation. H0.3 closed with `8a9a1c5 docs: mark H0.3 complete` and `8061da1 docs: finalize H0.3 handoff`; H0.4 closed with `f6cf177 docs: mark H0 health baseline complete` and `31bec78 docs: finalize H0 handoff`.
## Commits: `0b08f5a docs: freeze M5.1 integration contract`; `30cb781 feat(contracts): define session NPC presence`; `06859d4 feat(repo): add v43 NPC presence schema`; `04d7eba fix(contracts): make presence retries receipt-only`; `4cd41d3 feat(repo): add authoritative NPC presence`; `02eeec8 fix(contracts): preserve opaque presence sessions`; `49d6f02 feat(api): add session NPC presence routes`; `50c0403 feat(agent): use persisted NPC presence context`; `759a72c feat(client): manage session NPC presence`; `7094642 test(e2e): cover session NPC presence`; `3c4a2b6 fix(api): reject presence GET bodies`; `b07a355 docs: document session NPC presence`; `3b3772a docs: reconcile M5.1 architecture status`; `4479950 fix(repo): mask foreign presence sessions`; `35b763e docs: mark M5.1 complete`; `e943c1a docs: finalize M5.1 handoff`; `2ba2f6c docs: record Wave A implementation rechecks`.
## Files Modified: Lifecycle integration edits are limited to `docs/revision-2-integration-plan.md`, `docs/ROADMAP.md`, `devplan.md`, and `handoff.md`.
## Validation: The M5.2-M5.5 work was read-only analysis, so no code tests were required. Lifecycle consistency searches and diff checks cover this documentation update. Earlier M5.1 focused contracts, repository/routes/context/client tests, typechecks, deterministic E2E, and diff checks remain historical completion evidence; no totals are asserted here.
## Operational Notes: No schema beyond v43 is currently allocated. The M5.2 schema steward may allocate v44 only when implementation starts; M5.3 receives no schema only while limited to the fixed subset above, and M5.4/M5.5 pure contract/vector work receives no schema. M5.2-M5.6 implementations remain Planned, and protected `server/src/repo/contentCatalogRepo.ts` remains unchanged.
