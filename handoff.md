# Handoff
## Current: v45r1; 102 trusted-local RPG operations; M5.2 In Progress/partial after repository plus HTTP/client transport; M5.3 In Progress/partial after Slice 0 plus separate consumable HTTP/client UI; M5.4 and M5.5 pure no-schema protocol/vector checkpoints Complete; no full M5.2-M5.5 milestone completion claim
## Next Task: M5.2 minimal companion management UI if desired; M5.3 decide and freeze instant-modifier semantics or explicitly narrow the contract. M5.4 persistence/travel adapter and M5.5 persistence integration may proceed in parallel.
## Context: H0.1-H0.4 and Wave H are complete and green. Current persistence is v45r1, canonical populated v43/v44->v45 startup upgrades are supported, and v42 and earlier are unsupported. Preserve unrelated dirty work; do not inspect or modify `server/src/repo/contentCatalogRepo.ts`, stage, commit, push, clean, reset, stash, or overwrite another owner.
## Delivered Scope: M5.2 exposes fixed-local-owner authoritative companion management GET and closed receipt-only command POST for create companion/create grant/revoke grant, plus client transport only. M5.3 exposes separate consumable actions GET, command POST, and exact-result GET for exact pinned quantity-one damage/healing/health-guard-focus effects. The combat UI uses exact server target/action cost, persists ambiguity before POST, never automatically retries, and reconciles exact result plus combat/log/actions across reload. M5.4 and M5.5 retain their frozen protocol checkpoints.
## Retained Exclusions: M5.2 has no companion UI, public member HTTP, dismissal, proposal/decision administration, or grant exercise. M5.3 does not widen the legacy combat union; instant modifiers remain fail-closed because authoritative semantics are absent. Conditions, noninstant effects, nonconsumables, and spell slots are excluded. M5.4 persistence/adapters and M5.5 persistence/command/client integration remain Planned.
## H0 History: Root `npm run health` represents exactly `npm run typecheck && npm run build && npm test && npm run test:e2e`, fail-fast and once per phase. The final recovery run used a unique `/dev/shm` `TMPDIR` and passed all four phases in order after an external timeout and correction of a global foreign-key preflight regression. Independent release/security review found no material issues and confirmed deterministic/live credential isolation. H0.3 closed with `8a9a1c5 docs: mark H0.3 complete` and `8061da1 docs: finalize H0.3 handoff`; H0.4 closed with `f6cf177 docs: mark H0 health baseline complete` and `31bec78 docs: finalize H0 handoff`.
## Commits: `409103e feat(api): add companion administration lane`; `77505fb feat(app): expose companion and consumable actions`; `bab31cc fix(repo): share consumable action authority`; `441defc test(e2e): cover companion and consumable flows`; plus prior contract/schema/repository/rollback commits.
## Files Modified: Current documentation reconciliation is limited to the explicitly allowed root and `docs/` normative/lifecycle files; dirty `docs/README.md`, planning-board paths, package files, protected `server/src/repo/contentCatalogRepo.ts`, and unrelated files remain untouched.
## Validation: Deterministic E2E passed all 15 cases, including companion create/grant/revoke/replay and one response-after-commit consumable POST/reload with coherent custom immutable published-pack state. Fixture changes are harness-only and catalog attestation is unchanged. This docs-only reconciliation runs searches, local Markdown link checks, and diff validation only; no tests are run.
## Operational Notes: v45 is current. The two companion plus three consumable operations bring the explicit total from 97 to 102. Exposure alone does not close M5.2/M5.3.
