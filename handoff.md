# Handoff
## Current: v45r1; M5.2 In Progress/partial after repository-only administration; M5.3 In Progress/partial after Slice 0 and a consumable contract prerequisite; M5.4 and M5.5 pure no-schema protocol/vector checkpoints Complete; no full M5.2-M5.5 milestone completion claim
## Next Task: Implement the atomic connection-scoped consumable planner/executor. Add M5.2 HTTP administration contracts, routes, and client only after preserving the repository boundary. Keep M5.4 persistence/travel adapter and M5.5 persistence/command/client integration Planned.
## Context: H0.1-H0.4 and Wave H are complete and green. Current persistence is v45r1, canonical populated v43/v44->v45 startup upgrades are supported, v42 and earlier are unsupported, and the trusted-local RPG surface remains 97 explicit operations. Preserve unrelated dirty work; do not inspect or modify `server/src/repo/contentCatalogRepo.ts`, stage, commit, push, clean, reset, stash, or overwrite another owner.
## Delivered Scope: M5.2 repository-only administration creates companions from persisted present NPCs in attached running sessions, creates/revokes bounded grants, returns owner/GM management and member public projections, commits revision/idempotency/receipt/audit atomically, and replays for the exact issuer after demotion or membership removal. v45 replaces v44 sidecars preserving every row and uses durable principals for history; fresh authorization remains repository-derived current owner/GM. M5.3 Slice 0 remains delivered, and the consumable contract prerequisite now binds exact item/entry/target/effect plan/request digest/M15 revisions/result evidence with bounded policy. M5.4 and M5.5 retain their frozen protocol checkpoints.
## Retained Exclusions: M5.2 has no routes, UI, dismissal, proposal/decision administration, or grant exercise. M5.3 consumable runtime execution is not wired/live; existing route unions are unchanged. Conditions, non-instant effects, nonconsumables, and spell slots without an available level are excluded. M5.4 persistence/adapters and M5.5 persistence/command/client integration remain Planned. No autonomous NPC speech/action, inferred co-location, remote identity, simultaneous encounters, or additional HTTP operations.
## H0 History: Root `npm run health` represents exactly `npm run typecheck && npm run build && npm test && npm run test:e2e`, fail-fast and once per phase. The final recovery run used a unique `/dev/shm` `TMPDIR` and passed all four phases in order after an external timeout and correction of a global foreign-key preflight regression. Independent release/security review found no material issues and confirmed deterministic/live credential isolation. H0.3 closed with `8a9a1c5 docs: mark H0.3 complete` and `8061da1 docs: finalize H0.3 handoff`; H0.4 closed with `f6cf177 docs: mark H0 health baseline complete` and `31bec78 docs: finalize H0 handoff`.
## Commits: `ff2610c feat(contracts): define combat consumable protocol`; `42d9934 feat(repo): add v45 companion principals`; `839f70f feat(repo): administer companion grants`.
## Files Modified: Current documentation reconciliation is limited to the explicitly allowed root and `docs/` normative/lifecycle files; dirty `docs/README.md`, planning-board paths, package files, protected `server/src/repo/contentCatalogRepo.ts`, and unrelated files remain untouched.
## Validation: Documentation-only reconciliation: targeted lifecycle/schema/status searches, local Markdown link checks, and diff validation; no tests run and no commit created.
## Operational Notes: v45 is current. The M5.3 consumable prerequisite and M5.4/M5.5 checkpoints allocated no schema or HTTP operations. Treat contract completion as a frozen prerequisite, not runtime, route, client, or milestone completion.
