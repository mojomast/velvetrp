# Handoff
## Current: v45r1; M5.2 In Progress/partial after repository-only administration; M5.3 In Progress/partial after Slice 0 plus repository-only atomic consumable execution; M5.4 and M5.5 pure no-schema protocol/vector checkpoints Complete; no full M5.2-M5.5 milestone completion claim
## Next Task: In parallel, deliver M5.2 HTTP administration contracts/routes/client adding two operations and M5.3 route/client wiring for only the supported consumable runtime subset. Do not wire instant modifiers. Keep M5.4 persistence/travel adapter and M5.5 persistence/command/client integration Planned.
## Context: H0.1-H0.4 and Wave H are complete and green. Current persistence is v45r1, canonical populated v43/v44->v45 startup upgrades are supported, v42 and earlier are unsupported, and the trusted-local RPG surface remains 97 explicit operations. Preserve unrelated dirty work; do not inspect or modify `server/src/repo/contentCatalogRepo.ts`, stage, commit, push, clean, reset, stash, or overwrite another owner.
## Delivered Scope: M5.2 repository-only administration creates companions from persisted present NPCs in attached running sessions, creates/revokes bounded grants, returns owner/GM management and member public projections, commits revision/idempotency/receipt/audit atomically, and replays for the exact issuer after demotion or membership removal. v45 replaces v44 sidecars preserving every row and uses durable principals for history; fresh authorization remains repository-derived current owner/GM. M5.3 repository-only execution now derives separate legal actions/results and settles one exact pinned category-`consumable` unit in one immediate transaction. It preflights exact catalog/inventory/target/combat/M15/effect-plan/ID state before RNG, resolves ordered damage/healing/health-guard-focus effects, consumes inventory/action, aggregates acting/distinct-target M15 revisions, keeps actor/combat HP coherent, performs round-wrap M16 duration work, and commits turn/log/events/receipt with immutable replay and role-safe result lookup. M5.4 and M5.5 retain their frozen protocol checkpoints.
## Retained Exclusions: M5.2 has no routes, UI, dismissal, proposal/decision administration, or grant exercise. M5.3 has no route/client/E2E or live combat-union wiring; API remains 97 operations. Instant modifiers remain fail-closed despite contract eligibility because authoritative semantics are absent. Conditions, noninstant effects, nonconsumables, and spell slots are excluded. M5.4 persistence/adapters and M5.5 persistence/command/client integration remain Planned. No autonomous NPC speech/action, inferred co-location, remote identity, simultaneous encounters, or additional HTTP operations.
## H0 History: Root `npm run health` represents exactly `npm run typecheck && npm run build && npm test && npm run test:e2e`, fail-fast and once per phase. The final recovery run used a unique `/dev/shm` `TMPDIR` and passed all four phases in order after an external timeout and correction of a global foreign-key preflight regression. Independent release/security review found no material issues and confirmed deterministic/live credential isolation. H0.3 closed with `8a9a1c5 docs: mark H0.3 complete` and `8061da1 docs: finalize H0.3 handoff`; H0.4 closed with `f6cf177 docs: mark H0 health baseline complete` and `31bec78 docs: finalize H0 handoff`.
## Commits: `ff2610c feat(contracts): define combat consumable protocol`; `42d9934 feat(repo): add v45 companion principals`; `839f70f feat(repo): administer companion grants`; `e27402d feat(repo): settle combat consumables atomically`; `cd885dc test(repo): align v45 corruption assertions`; `cd8f842 test(repo): cover consumable rollback boundaries`; `2e1947b docs: document v45 companion administration`; `f33f2e4 docs: record resumed M5 slices`; `9c7e7f1 docs: finalize resumed M5 handoff`.
## Files Modified: Current documentation reconciliation is limited to the explicitly allowed root and `docs/` normative/lifecycle files; dirty `docs/README.md`, planning-board paths, package files, protected `server/src/repo/contentCatalogRepo.ts`, and unrelated files remain untouched.
## Validation: Committed focused repository coverage validates exact server-derived consumable execution, ordered settlement/replay, duplicate generated-ID cardinality, transaction-boundary rollback, and reopen/result lookup. `cd885dc` corrects existing corruption-fixture assertions to schema marker 45. Prior consumable contract tests/contracts typecheck and v45 migration/support-window, companion, NPC-presence, marker, and server-typecheck gates passed. This docs-only reconciliation runs searches, local Markdown link checks, and diff validation only; no tests are run.
## Operational Notes: v45 is current. M5.3 repository runtime and M5.4/M5.5 checkpoints allocated no schema or HTTP operations. Treat repository runtime as a bounded prerequisite, not route, client, E2E, or milestone completion.
