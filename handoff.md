# Handoff
## Current: v44r1; M5.2 In Progress/partial; M5.3 Slice 0 delivered/partial; M5.4 and M5.5 pure no-schema protocol/vector checkpoints Complete; no full M5.2-M5.5 milestone completion claim
## Next Task: Continue M5.2 repository administration core and M5.3 consumable prerequisites/policy, especially exact inventory/action transaction contracts. Keep M5.4 persistence/travel adapter and M5.5 persistence/command/client integration gated by their domain designs.
## Context: H0.1-H0.4 and Wave H are complete and green. Current persistence is v44r1, canonical populated v42/v43->v44 startup upgrades are supported, v41 and earlier are unsupported, and the trusted-local RPG surface remains 97 explicit operations. Preserve unrelated dirty work; do not inspect or modify `server/src/repo/contentCatalogRepo.ts`, stage, commit, push, clean, reset, stash, or overwrite another owner.
## Delivered Scope: M5.2 contracts plus additive empty v44 immutable revisioned commands/receipts and companion, presence-link, proposal, decision, grant, revocation, audit, and attestation sidecars; no repository administration, routes, UI, or grant exercise. M5.3 Slice 0 makes active encounter HP authoritative, mirrors actor health atomically through M15, fails health-mutating external commands closed, advances round effects only at wrap, and retains concentration until replacement/removal. M5.4 freezes exact-candidate v1 and vectors with first kind `actor.travel`. M5.5 freezes exact framing, SHA-256/HKDF/HMAC/rejection sampling, mandatory observed->contributed capability state, lifecycle, and proof vectors.
## Retained Exclusions: M5.3 consumable Slice 1 remains Planned/blocked; M5.4 persistence/adapters and M5.5 persistence/command/client integration remain Planned. No delegated grant exercise before L5, autonomous NPC speech/action, inferred co-location, remote identity, simultaneous encounters, or additional HTTP operations.
## H0 History: Root `npm run health` represents exactly `npm run typecheck && npm run build && npm test && npm run test:e2e`, fail-fast and once per phase. The final recovery run used a unique `/dev/shm` `TMPDIR` and passed all four phases in order after an external timeout and correction of a global foreign-key preflight regression. Independent release/security review found no material issues and confirmed deterministic/live credential isolation. H0.3 closed with `8a9a1c5 docs: mark H0.3 complete` and `8061da1 docs: finalize H0.3 handoff`; H0.4 closed with `f6cf177 docs: mark H0 health baseline complete` and `31bec78 docs: finalize H0 handoff`.
## Commits: `9efd346 feat(contracts): define companion core`; `7c41064 feat(repo): add v44 companion foundation`; `a2b479c feat(contracts): freeze exact candidate protocol`; `3d834f4 feat(server): freeze verifiable dice protocol`; `68b3c57 feat(repo): compose authoritative combat health`; `45ec7cc docs: document v44 Wave A foundation`; `41f1476 docs: record Wave A slice status`.
## Files Modified: Current documentation reconciliation is limited to the explicitly allowed root and `docs/` normative/lifecycle files; dirty `docs/README.md`, planning-board paths, package files, protected `server/src/repo/contentCatalogRepo.ts`, and unrelated files remain untouched.
## Validation: Focused companion and exact-candidate contracts, v44 migration/support-window, combat composition, verifiable-dice, and corruption-startup tests passed; contracts and server typechecks passed. Targeted lifecycle/schema/status consistency searches, local Markdown link-target checks, and `git diff --check` also passed.
## Operational Notes: v44 is current. M5.3 Slice 0 and M5.4/M5.5 checkpoints allocated no schema. Treat checkpoint completion as a frozen contract boundary, not persistence, adapter, route, client, or milestone completion.
