# Handoff
## Current: M5.1 session NPC presence In Progress; not delivered
## Next Task: Parallel disjoint new contract files/tests and sole schema-steward v43 migration/fixture work. Shared barrels/wiring are reserved for the integration owner; protected `server/src/repo/contentCatalogRepo.ts` remains untouched.
## Context: H0.1-H0.4 and Wave H are complete and green. M5.1 is In Progress; M5.2-M5.6 remain Planned. Preserve unrelated dirty work; do not inspect or modify `server/src/repo/contentCatalogRepo.ts`, stage, commit, push, clean, reset, stash, or overwrite another owner.
## Start Recheck: Read-only contract/code/schema/fixture inspection confirms current v42r1, no v43 or NPC-presence artifact drift, intact canonical v40/v41 support fixtures, provisional v43r1 available, and no blockers.
## Implementation Policy: Stopped-session projection is structurally historical at-stop and writes reject after stop. Detach blocks only a running session with live-present rows and preserves historical presence. Player location uses the existing principal-visible location union with no actor inference. Commands use session-root `expectedRevision`; per-NPC revision is informational. Authority and transitions execute in one immediate repository transaction; DDL enforces structural integrity only.
## H0.4 Evidence: Root `npm run health` represents exactly `npm run typecheck && npm run build && npm test && npm run test:e2e`, fail-fast and once per phase. The final recovery run used a unique `/dev/shm` `TMPDIR` and passed all four phases in order. Independent release/security review found no material issues and confirmed deterministic/live credential isolation.
## Operational History: The first attempt timed out externally; the second exposed a global foreign-key preflight regression, which was corrected and scoped before the final wrapper passed.
## Commits: `3e6c1a6 fix(repo): scope migration integrity preflight`; `f18a081 chore: add canonical health gate`; `7286ded docs: document canonical health gate`; `f6cf177 docs: mark H0 health baseline complete`. Nothing was staged or committed by this lifecycle integration owner.
## Files Modified: Lifecycle integration edits are limited to `docs/revision-2-integration-plan.md`, `docs/ROADMAP.md`, `devplan.md`, and `handoff.md`.
## Validation: Targeted lifecycle status/consistency, scoped diff, and `git diff --check` checks passed for this docs-only start update. No feature tests or health phase ran, and no M5.1 validation is claimed.
## Operational Notes: The exact next operation is parallel disjoint new contract files/tests and sole schema-steward v43 migration/fixture work, with shared barrels/wiring reserved for the integration owner. Protected `server/src/repo/contentCatalogRepo.ts` remains untouched. No M5.1 shipped or feature-validation claim is made.
