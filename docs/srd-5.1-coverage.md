# SRD 5.1 Ruleset Coverage

The `dnd-5e@1.0.0` development ruleset is a pure, swappable mechanics module adapted from the 2014 SRD 5.1. It is a deliberately incomplete implementation, not full D&D support or full SRD conformance. Callers inject every die face; functions return immutable resolutions or proposed state-change plans and never access a database, network, provider, clock, or global random source.

This work includes material taken from the System Reference Document 5.1 (SRD 5.1) by Wizards of the Coast LLC. The [official SRD source page](https://www.dndbeyond.com/srd) provides the exact [SRD 5.1 Creative Commons PDF](https://media.dndbeyond.com/compendium-images/srd/5.1/SRD_CC_v5.1.pdf). The SRD 5.1 is licensed under [Creative Commons Attribution 4.0 International (CC BY 4.0)](https://creativecommons.org/licenses/by/4.0/legalcode). VelvetRP adapts and modifies the source material by selecting a limited rules subset and expressing it as deterministic software mechanics; Wizards of the Coast LLC has not endorsed these modifications.

The Training Dummy is an original Velvet deterministic integration fixture, not SRD material. Release attribution and modification details are in [`NOTICE.md`](../NOTICE.md).

The recommended development starter is the exact `srd-5.1:rules:starter-v1` profile and `srd-5.1:starter@1.6.0+5bb1aaf8154c` publication. Its catalog carries all twelve SRD classes at levels 1-20 with one subclass each, the full SRD 5.1 spell list (levels 0-9), every SRD ancestry and the Acolyte background, the SRD equipment and magic-item sets, and SRD monsters across every challenge rating. Execution remains bounded: Fighter Second Wind, Barbarian Rage, Paladin Lay on Hands, Cleric Bless, Cure Wounds, and Healing Word, and Wizard Magic Missile, False Life, and Shield execute through server-owned commands; expanded spells, monster traits, and magic-item powers are attribution-visible catalog metadata unless an executable lane exists. Ranged and thrown weapons, full-cover geometry, and opportunity reactions execute when tactical positions, reach, and ammunition are authoritative. All nine SRD ancestries are catalog-backed with their resistance and trait metadata. The prior publications remain immutable. Existing Velvet starter campaigns remain bound to `velvet-starter-v1@1.0.0` through the behavior-preserving legacy adapter. A campaign binding is immutable after mechanics descendants exist. Resolution verifies the rules profile binding, compiled module version, and every exact publication; unknown or mixed identities fail closed.

New campaign setup presents this SRD starter as the recommended mechanical profile. Campaign generation receives the exact trusted `srd-5.1:rules:starter-v1` and `dnd-5e@1.0.0` identity plus only the campaign's pinned public catalog references. Generated encounters, quest items, and monster concepts may reference those exact definitions but cannot invent mechanics or create executable entities. Adventure planning and narration use the selected campaign descriptor rather than a global D&D default.

The descriptor's `capabilities` array is the machine-readable compatibility surface. Each capability is independently versioned. The module version changes when this combined surface changes; capability versions allow integrations to depend on narrower behavior.

## Machine-Readable Inventory

[`srd-5.1-coverage.v1.json`](srd-5.1-coverage.v1.json) is the authoritative versioned coverage inventory. It records major SRD rules and content domains using only `implemented`, `partial`, `unsupported`, or `not-applicable`; links section identifiers to the exact official PDF; and identifies capability, runtime, test, and API evidence without reproducing SRD rules text. The `complete` field is a bounded inventory claim, not a claim of complete D&D or SRD conformance.

## Coverage Matrix

| Area | Status | Capability | Implemented behavior | Deliberate limits |
| --- | --- | --- | --- | --- |
| Ability modifiers and proficiency | Supported | `checks@1.0.0` | Scores, level 1-20 proficiency, proficiency/half/expertise rounding | No class-specific substitutions |
| Ability, skill, and saving throws | Supported | `checks@1.0.0` | All 18 SRD skills, DC comparison, flat bonuses, typed results | Contests and group checks require caller orchestration |
| Advantage/disadvantage | Supported | `checks@1.0.0` | Two injected d20s, any opposing sources cancel, evidence records selection | Elven Accuracy and reroll features unsupported |
| Passive checks | Supported | `passive-checks@1.0.0` | Base 10, proficiency multiplier, +/-5 for advantage state | Situation-specific modifiers are caller supplied |
| Attacks and critical hits | Partial | `attacks@1.5.0` | Attack bonus vs AC, natural 1 miss, natural 20 or configured 2-20 threshold critical, server-derived melee/ranged/finesse/thrown properties, long-range and within-5-feet-of-a-hostile disadvantage, ammunition, half/three-quarters cover AC, full-cover blocking, tactical line-of-effect gates, the always-available proficient unarmed strike (1 + Strength modifier bludgeoning), grapple/escape/shove contests, and SRD spell attacks or saving-throw spells resolved from the caster's casting ability | Complete concealment and reach variants remain unsupported; feature-specific riders resolve through the on-hit rider pipeline |
| Damage rolls | Supported | `damage@1.0.0` | Multi-term injected dice evidence, doubled critical dice, modifier once, minimum zero | Rerolls, minimum-die features, damage type interactions unsupported |
| Damage adjustments and temporary hit points | Partial | `damage@1.3.0` | One explicit normal/resistance/vulnerability/immunity adjustment, shared by powers, weapon attacks, enemy turns, and opportunity attacks from authoritative effect modifiers or the pinned enemy definition; temporary hit points absorb damage before hit points and never stack (a new grant keeps the larger pool) | Multiple simultaneous damage types and out-of-combat temporary HP remain unsupported |
| Initiative | Supported | `initiative@1.0.0` | Injected rolls, modifiers, stable deterministic ordering | SRD player/GM tie choice represented by deterministic Dexterity then ID policy; surprise unsupported |
| Speed and movement | Partial | `movement@1.5.0` | Pure speed/movement plans; authoritative tactical grid paths and difficult terrain spend the persisted current-turn allowance; the turn-start allowance applies the SRD encumbered (-10) and heavily encumbered (-20, check/attack disadvantage) thresholds; Dash spends the action and adds one speed to the persisted allowance for the turn; standing up from prone spends half the allowance and clears the condition without using an action; Disengage and opportunity reactions are server-derived; Help grants a `helped` marker the ally's next attack consumes, and Hide makes a Dexterity (Stealth) check against opposing passive Perception to set a `hidden` marker (`combat-markers@1.0.0`), both granting attack advantage | Hidden does not yet suppress detection or impose unseen-attacker rules beyond advantage; long/high jumps, climbing, swimming, squeezing, flying, and falling are derived as pure plans but not yet integrated into tactical movement spending; mounts and full reach geometry remain unsupported |
| Short and long rests | Partial | `rests@1.1.0` | Camp-gated 60-minute short rests, hit-die spending, HP caps, long-rest HP/hit-die recovery, one exhaustion reduction, unsafe-camp rejection, active-combat rejection, durable server-time intervals, and rest-recovery bindings that recharge spell slots and long-rest class pools (Rage, Lay on Hands) created at character creation; every modeled class level grants one hit die so the hit-dice pool tracks total character level | No food/water accounting, travel pace variants, or explicit interruption duration |
| Concentration | Partial | `concentration@1.1.0` | Damage DC, Constitution save, replacement plan, explicit environmental DCs, caster-controlled ending, and incapacitation/death/0-HP triggers | Broader feature interactions remain caller-orchestrated |
| Conditions | Partial | `conditions@1.2.0` | Immutable add/remove plans for 15 SRD conditions; attack-roll effects (blinded/poisoned/prone/restrained disadvantage; blinded/restrained/stunned/unconscious/prone advantage; melee auto-crit against unconscious/paralyzed/petrified) applied to weapon attacks, enemy turns, and opportunity attacks | Non-attack condition effects are automated for the listed conditions; broader feature interactions remain caller-orchestrated |
| Exhaustion | Partial | `exhaustion@1.0.0` | Levels 0-6: ability-check disadvantage (1), speed halved (2), attack/save disadvantage (3), hit-point maximum halved (4), speed 0 (5). Speed applies to the turn-start allowance and attack disadvantage to weapon, enemy-turn, and opportunity attacks; long rest reduces one level | Check/save disadvantage and hit-point maximum are derived but not yet enforced; level 6 death is not automatic |
| Generic resources | Supported | `resources@1.0.0` | Atomic, aggregated resource affordability and resulting-pool plans | Recharge timing is caller-controlled |
| Spell execution | Partial | `spell-costs@1.2.0` | Cleric Bless, Cure Wounds, Healing Word, and Wizard Magic Missile, False Life, and Shield enforce preparation, components, level-one slots, legal targets, ranged distance/line-of-effect/full-cover gates where applicable, concentration/healing/damage/temporary-hit-point/defense-modifier effects, server RNG, stale revisions, idempotency, and restart replay; a leveled spell can consume an explicit higher `slotLevel`, validated by `planDnd5eSpellCost`, and scales (Cure Wounds/Healing Word gain a die, Magic Missile gains a dart per level). Shield is a reaction that adds +5 armor class until the next round wrap, applies to weapon, spell, enemy, and opportunity attacks, and can be triggered at true hit time | The full SRD spell catalog is present as bounded metadata; per-spell execution beyond the listed lanes, ritual casting, material inventory, and casting-time variants remain unsupported |
| Character derived values | Partial | `derived-values@1.0.0` | Ability modifiers, proficiency, armor formula inputs, initiative, passive Perception, speed, pinned ancestry bonuses, class hit dice, and carrying/encumbrance projections | Multiclassing, feats, complete saves, and feature interactions remain partial |
| Development character creation | Partial | `derived-values@1.0.0` | Six SRD abilities; standard array, 27-point buy, and server 4d6-drop-lowest; seven bounded classes, nine pinned ancestries, Acolyte, class resources, and Cleric preparation persist durably | Twelve classes (levels 1-20 with one subclass each) are catalog-backed; the SRD contains only the Acolyte background; optional rules beyond the authored subclass choices remain selection-only and feats execute as bounded metadata |
| Campaign actor checks | Partial | `checks@1.0.0` | Ability, save, and all semantic SRD skill IDs resolve from the authoritative sheet with campaign-owned RNG and named DCs | Opposed SRD checks and situational feature exceptions are unsupported |
| Repository combat actions | Partial | `attacks@1.0.0` | Exact pinned equipment profiles support Strength/Dexterity/finesse, shields, versatile/two-handed grip state, critical dice, atomic HP, idempotent receipts, grapple/escape contests, Dash, Disengage, Help, Hide, Second Wind, ranged/thrown weapons, ammunition, half/three-quarters/full cover geometry, and restart replay. Server-owned enemy turns execute catalog-pinned basic attacks for the SRD monster set across every challenge rating (plus the Training Dummy fixture), with Wolf knockdown and Goblin escape evidence. | Reach variants and complete condition-effect evaluation remain partial; no client enemy target choices |
| Repository turn economy | Partial | Repository integration | Durable turn identity, action/bonus-action/reaction costs, explicit end-turn, legal-action filtering, grapple/utility actions, opportunity reactions, replay and restart; atomic tactical movement spending and combat revision updates | Help/Hide benefits are not yet carried into later rolls; the full reaction catalog remains incomplete |
| Equipment profiles | Partial | Catalog and repository integration | Typed SRD weapons, armor, shields, tools, ammunition, weight, proficiency, exact hand/grip state, versatile/two-handed rules, armor requirements, stealth disadvantage, carrying limit, and live sheet/combat AC/attack derivation | Ammunition is bounded to explicit resource stacks; item effects, full tool use, and complete encumbrance movement penalties remain unsupported |
| Repository Fighter rests | Partial | `rests@1.1.0` | Durable level-one d10 pool and exhaustion resource; explicit short-rest die selection, server RNG, atomic capped healing; long-rest HP/dice/exhaustion recovery, 24-hour receipt limit, and replay | Duration, interruption, sleep, sustenance, camp safety, and automatic exhaustion effects are not enforced |
| Survival at zero HP | Partial | Repository integration | D&D actor 0 HP becomes unconscious; durable death saves, stable/dead states, server-rolled saves, damage failures, exact ally stabilization, healing recovery, replay and restart | Stabilization has no range/adjacency check; massive damage and medicine-check stabilization are enforced; revivification and broader condition effects remain unsupported |
| Legal action envelope | Supported | `legal-action-plans@1.0.0` | Deterministic `legal`, `reasons`, and nullable `result` shape | Action economy and encounter legality need domain state from the caller |

## Unsupported SRD Areas

The module does not claim full SRD conformance. The catalog now carries the complete SRD 5.1 spell list, all twelve classes with one subclass each, every ancestry and the Acolyte background, the magic-item set, and SRD monsters across every challenge rating, but most of that content is bounded, attribution-visible metadata rather than executable rules. Still unsupported as executable behavior: broad spell saves/attack effects, subclass and class-feature execution beyond bounded metadata, feat selection, alternate backgrounds, concealment/visibility/surprise, mounted and underwater combat, full monster stat blocks (each monster exposes one basic attack plus trait markers), encounter building, treasure, downtime, food/water, and travel hazards. Generic SRD damage powers fail closed rather than bypass authoritative attack checks. SRD progression commands fail closed and never use Velvet formulas.

## Integration Contract

Repositories resolve through the exact `(rulesetId, rulesetVersion)` registry key and `campaign_ruleset_bindings_v60`, verify profile/publication agreement, verify the required descriptor capability, and invoke the optional typed `module.mechanics` surface. The repository remains responsible for authorization, current-state lookup, RNG, idempotency, transaction boundaries, durable receipts, and applying a legal plan atomically. Modules without `mechanics` support only the retained legacy API.

Reviewed campaign setup configures the exact publication and pins every publicly reachable ability and spell of that publication for execution in `rpg_campaign_catalog_definitions_v25`. Only pinned definitions may be planned or executed; the pin set is idempotent and campaign-scoped.

1. Build inputs from authoritative state and caller intent.
2. Obtain random die faces outside the ruleset and pass them as `rolls` arrays.
3. Call a pure resolver or planner exported by `server/src/rulesets/index.ts`.
4. Reject plans where `legal` is false, exposing `reasons` as appropriate.
5. Persist the input/evidence, result or plan, ruleset ID/version, capability version, and resulting state in one repository transaction.

Do not infer mechanics from `supportedMechanics`; use the versioned `capabilities` manifest and the typed function API. The legacy `resolveCheck`, `abilityModifier`, and `proficiencyBonus` module methods remain available for existing repositories.
