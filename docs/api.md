# API Reference

All routes are under `/api`. JSON write requests use `Content-Type: application/json`.

This page specifies HTTP behavior. Internal module locations below are implementation references only and do not add to or modify any API contract.

Structured and normal HTTP responses include `X-Request-Id`. Intentionally preserved raw pre-router malformed/overlong responses for unknown, legacy, and compatibility paths may omit that header; their existing raw compatibility behavior is not promoted to the structured response contract. A caller may supply an ID using `X-Request-Id` when it is 1-128 characters containing only letters, digits, `.`, `_`, `:`, or `-`; invalid values are replaced. Existing roleplay response bodies are unchanged and do not include request metadata. Structured campaign problem `instance` strings use safe route templates rather than concrete campaign IDs or caller-controlled lookalike suffixes and never include queries. Production automatic request logs keep method and an available safe route template only; when routing context is unavailable, the concrete path is omitted.

## General

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/health` | Server health |
| `GET` | `/features` | Voice and image feature flags |
| `GET`, `PUT` | `/harness` | Read or update prompt harness settings |
| `GET`, `PUT` | `/provider` | Read public provider settings or update persisted settings |
| `GET` | `/usage` | Read lifetime token totals, breakdowns, and estimated cost |
| `GET` | `/prompt-templates` | List every underlying prompt template, placeholders, defaults, and effective overrides |
| `PUT` | `/prompt-templates/:id` | Save a prompt override or reset with `{ "template": null }` |

`GET /rpg/v1/features` exposes the M0 RPG boundary as `{ campaign, mechanics, combat, studio, remoteAuthentication }`. Every flag defaults to `false` and is enabled only by the exact value `true` in `FEATURE_RPG_CAMPAIGN`, `FEATURE_RPG_MECHANICS`, `FEATURE_RPG_COMBAT`, `FEATURE_RPG_STUDIO`, or `FEATURE_REMOTE_AUTHENTICATION`. These are rollout controls, not authorization.

Current persistence is schema v25 revision 1. V25 completes the M1.5 repository/shared-contract layer for actor resource sidecars, inventory/equipment, integer-minor wallets and currency ledgers, shops/finite stock/quotes/purchases, bilateral trade, and short/long rest; its canonical DDL digest is `a5e3a58f8014978315d20440a0ac087871edac95323d059327faa2fe0a983ef7`. This reference exposes exactly 21 trusted-local RPG HTTP operations: the historical 14 plus character-draft POST/GET/PATCH, progression GET/preview POST, and administration GET/PATCH. These routes are server-only and have no client/UI. M1.5 remains repository-only: it adds no routes or client/UI. Builder/progression require campaign plus mechanics flags; administration requires campaign.

**Historical Slice 98 checkpoint:** exactly 98 M0 slices were complete at v14r1 and exactly 13 campaign HTTP operations. Slice 98 was documentation-only closeout and added no feature, code, test, contract, route, operation, schema, migration, dependency, backup, or commit. Its gate passed typecheck, build, 1,993 unit tests plus 1 skip, and 1 deterministic E2E; live E2E was not run. This preserved ledger describes that checkpoint, not current persistence.

When `FEATURE_RPG_CAMPAIGN=true`, `GET /rpg/v1/campaigns` returns the strict shape `{ "campaigns": CampaignAccess[] }` in repository order. It accepts no query parameters; supplied parameters receive structured `RPG_INVALID_REQUEST`. Repository authorization first requires valid principal/campaign parents and a canonical recognized-role membership; a purported owner must also match `campaign.owner_principal_id`. Once authorized, every role requires exactly one strict owner membership matching that pointer and an intact owner-principal parent. Missing, duplicate, mismatched, malformed, or orphaned owner state fails loudly for authorized owner/GM/player/observer callers. Stale purported owners, missing authorization parents, and outsiders remain non-disclosing. Repository exceptions, repository-open failures, and response-schema failures return a redacted structured `RPG_INTERNAL_ERROR` 500 with the request ID. A failed repository initialization is cached and is not retried until a new server/plugin instance is constructed.

`POST /rpg/v1/campaigns` accepts the strict JSON body `{ "name": string }`, using the shared trimmed 1-200 character campaign-name rule, and returns `201` with exactly `{ "campaign": Campaign }`. It emits no `Location` header and accepts no query parameters. The media type must be exactly `application/json`, optionally with a `charset` parameter. Missing or other media types receive structured `RPG_UNSUPPORTED_MEDIA_TYPE` 415; malformed, empty, and schema-invalid bodies receive stable structured `RPG_INVALID_REQUEST` 400 responses. Only a mismatch against a complete, valid application-owner relation is a redacted 403; missing, malformed, or orphaned owner invariant state remains a redacted generic 500 without dependency use or writes. A generated campaign/timeline ID collision is a redacted 409. Dependency, SQL, projection/output, ordinary, and lookalike errors also remain redacted generic 500s. There are no retries or idempotency guarantees: if a client loses the POST response, whether the campaign committed is ambiguous, so it must inspect the authoritative list rather than automatically retrying the POST.

`GET /rpg/v1/campaigns/:campaignId` accepts one strict 1-128 character resource ID and no query parameters. Every success and scoped problem carries `Cache-Control: no-store`, including feature-denied and normalized malformed/overlong exact targets; the client likewise requests this authoritative projection with `cache: "no-store"`. Missing, denied, stale-owner, ordinary invalid, overlong, and invalid-percent campaign paths all return the same request-correlated `RPG_CAMPAIGN_NOT_FOUND` 404 problem when enabled, including `X-Request-Id`. Success returns exactly `{ "campaign": { "id", "name", "actorRole", "createdAt", "updatedAt", "content" } }`; `content` is either `{ "status": "unconfigured" }` or `{ "status": "configured", "rulesProfileId", "contentPacks": [{ "packId", "packVersion" }] }`. Pack identifiers are deterministic and exact, configured state requires a complete selected-profile/sealed-compatible-pack graph, duplicate pack IDs reject, and `updatedAt` cannot precede `createdAt`. The response never contains `ownerPrincipalId` or `activeTimelineId`.

`GET /rpg/v1/campaigns/:campaignId/rooms` uses fixed `local-owner`, one strict campaign path, no query, and `no-store`. Its exact response is `{ "attached": AttachedRoomSummary[], "eligible": RoomSummary[] }`; each list is independently capped at 1,000 with MAX+1 rejection. A room summary contains only exact opaque untrimmed `sessionId`, nullable safely projected title, 1–12 safely projected participant names, and `createdAt`; attached summaries additionally contain `attachedAt` and `stopped`. Attached order is attachment time then binary session ID; eligible order is creation time then binary ID. Lists reject duplicates, overlap, malformed UTF-16 display output, unknown/private fields, and wrong order. One explicit-column statement gives factory callers a statement snapshot and UoW callers their enclosing snapshot. A materialized SQL authorization gate requires actor principal ancestry, a recognized role, canonical actor/owner membership timestamps, owner-pointer agreement for an owner actor, and the canonical exact sole parent-backed owner membership plus `owner_role` before attached/eligible candidates or title/participant presentation can materialize. Missing principal ancestry, unknown actor roles, stale purported-owner pointers, malformed preauthorization, and outsiders therefore feed no presentation subquery. Denied/unattributable states remain masked, while strict JS validation after authorization makes attributable malformed authority loud. Attached rooms require intact ancestry, complete unique contiguous 1–12 participants including exactly one primary, safe projections, and either recognized running state with no stop provenance or canonical closed state with complete stop provenance; eligible rooms require the running form, global nonattachment, and exact owner. Foreign campaigns remain isolated. Character IDs, boundaries, safe words, messages, context, presets, and stop-reason values are never emitted; only stop-provenance validity metadata is inspected. Missing/denied campaigns are 404; authorized malformed output/repository failures are neutral redacted 500s.

The GET authorization CTE additionally requires the campaign owner ID, sole owner-membership ID, and owner-parent ID to each be SQLite text, binary-equal, NUL-free strict 1–128-character resource IDs before any presentation CTE. Whitespace and every character outside letters, digits, `.`, `_`, `:`, and `-` are rejected; strict JS validation remains after authorization.

`PUT /rpg/v1/campaigns/:campaignId/rooms` accepts exact JSON `{ "sessionId": string }`; the session ID is opaque, untrimmed, and not resource-ID validated. Success is `200 { "attachment": { "sessionId", "attachedAt" } }` with exact request/path binding and no `Location`. Under one `BEGIN IMMEDIATE`, exact pointer/`owner_role`/sole-owner-membership/principal authority for the requested campaign is established first, then the attachment relation is inspected before the target session graph. Any attachment whose raw campaign differs returns the same generic typed 409 without validating or disclosing foreign campaign/session lifecycle, participant, ancestry, timestamp, or other corruption. A same-requested-campaign attachment is strictly parsed and its complete session graph validated before idempotent return; an unattached room's complete running eligibility is validated before insertion. Attributable orphan/malformed attachment or graph state remains an untyped neutral 500. Same-campaign idempotency preserves the original timestamp and consumes no clock after a later valid stop. Missing/denied campaign and genuinely absent unattached room are typed 404s; foreign attachment or complete canonical stopped-new-attachment is typed 409. SQL, malformed output, and other unexpected errors are likewise commit-ambiguous 500s requiring authoritative room GET reconciliation and never automatic PUT retry. Query, media, malformed-body, feature, method-normalization, cache, problem, and operation logs never reflect the body/session ID; HEAD and methods other than GET/PUT are absent and every scoped RPG miss/unsupported method carries `Cache-Control: no-store`.

`PATCH /rpg/v1/campaigns/:campaignId` is the owner-only stale-safe rename boundary. It accepts exactly `{ "name": string, "expectedUpdatedAt": UtcIsoTimestamp }`, requires exact JSON (optionally one `charset` parameter), accepts no query parameters, and returns exactly `{ "campaign": { "id", "name", "updatedAt" } }`. IDs are strict resource IDs and timestamps are canonical millisecond UTC values; successful `updatedAt` is strictly greater than `expectedUpdatedAt`, never equal. Missing and denied campaigns share `RPG_CAMPAIGN_NOT_FOUND` 404; stale writes return typed 409; all unexpected failures are redacted 500s. The repository validates complete ownership and the precondition in one immediate transaction and never retries automatically.

`PUT /rpg/v1/campaigns/:campaignId/starter-setup` is the explicit trusted-local original-starter operation. It accepts no query and exactly `{ "starterId": "velvet:original-starter@1.0.0+d15042935818" }`; no other ID or caller content is accepted. The accepted ID is derived from the same exact pack ID/version constants used by the recursively frozen shared presentation and server manifest, preventing identity drift. Success is the existing minimal authoritative `CampaignDetailResponse` proving the exact fixed profile and one exact pack. Missing/denied state is non-disclosing 404; a different configuration, any reserved exact profile/pack/definition collision, or an exact starter configuration whose required reserved profile or pack is missing/malformed is typed 409 without repair; malformed requests are 400 and unexpected failures are request-correlated redacted 500s. HEAD is disabled and spoofed identity is ignored.

`PUT /rpg/v1/campaigns/:campaignId/mechanics-starter-setup` is a distinct trusted-local operation requiring both campaign and mechanics feature flags. It accepts no raw query delimiter (including bare `?`) and exactly `{ "starterId": "velvet:mechanics-starter@1.1.0+2f9199b5696d" }`; caller identity, catalog/profile/pack/revision/command/idempotency fields, and unknown fields reject. Feature denial precedes query, path, media, body, and lazy repository access; enabled validation order is query, strict path, exact JSON media, then strict body. HEAD is absent, every scoped response is `no-store`, success is `200` with no `Location`, and the strict minimal campaign response is path-bound to the exact mechanics profile and sole exact pack.

The server fixes `local-owner`, observes the campaign administration revision itself, and owns the fixed idempotency identity. Each pre-write classification and post-write proof is one short repository read transaction; no read snapshot spans either write. It calls the existing catalog publication and campaign configuration repository methods as two real transactions, performs no retry, and authoritatively classifies final state after success or failure. Fresh activation is restricted to unconfigured exact-owner campaigns; exact validated active mechanics configuration converges without writes; original-starter, legacy lookalike, or any other configuration is a stable 409 and is never migrated, repaired, or replaced. Missing/denied state is non-disclosing 404 and unexpected/open/integrity/output failures are correlated redacted 500s. The client places both starter choices under one document-lifetime campaign mutation lock. One action issues at most one PUT, then every issued outcome receives exactly one fresh no-store campaign-detail GET; successful reconciliation invalidates older detail generations so late pre-activation reads cannot restore setup controls. The PUT is never automatically repeated, and only exact HTTP 200 is accepted as a mechanics setup success.

`GET /rpg/v1/campaigns/:campaignId/characters/creation-options` is the owner/GM-authorized trusted-local campaign-character creation-options read. The HTTP route always uses fixed unauthenticated trusted-local `local-owner`; callers cannot select or spoof a principal, and the repository determines whether that fixed principal has parent-backed owner/GM creation authority. It accepts one strict 1–128 character campaign resource ID and no query parameters. It returns exactly `{ campaignId, personas, starter }`, where `personas` preserves repository order and contains only bounded unique `{ characterId, name, alreadyUsed }` summaries, and `starter` contains only the fixed basic finalized profile, pack, race, background, and level-1 class metadata. Safe persona-name contracts require nonblank 1–200-code-unit well-formed UTF-16 while retaining valid astral pairs; malformed repository names become redacted 500s. It does not expose full legacy character profiles, controller IDs, private notes, sheets, actors, campaign ownership, or timelines. The response is strictly parsed and `campaignId` must equal the decoded path ID. Literal repository `null`, including missing or denied owner/GM state, maps to the same non-disclosing `RPG_CAMPAIGN_NOT_FOUND` 404; `undefined`, other falsey values, and all other malformed output map to a redacted `RPG_INTERNAL_ERROR` 500, as do repository-open, repository, and wrong-path-output failures. For malformed-percent or overlong exact detail GET/PATCH, starter PUT, and creation-options GET targets, feature denial remains first; when enabled, any raw query is a structured query-free-instance `RPG_INVALID_REQUEST` 400 before campaign-ID 404. An unsupported method against the same malformed/overlong exact resource shape remains absent as correlated `RPG_ROUTE_NOT_FOUND`, does not open the repository, and discloses neither its query nor resource ID. Raw unknown, legacy, and lookalike router responses retain their Fastify-compatible JSON/code/status with generic messages that reflect neither path nor query. HEAD and all non-GET methods are absent.

`GET /rpg/v1/campaigns/:campaignId/characters` is the trusted-local safe roster read. It accepts one strict 1–128 character campaign resource ID and no raw query delimiter, including a bare `?`, and always calls `getCampaignCharacterRoster("local-owner", campaignId)`. The factory/UoW method uses one explicit-column SQLite statement and preserves existing parent-backed owner/GM/player/observer list authorization, including stale-owner and outsider masking. Authorized empty state returns non-null `{ campaignId, characters: [] }` internally and public `200 { "characters": [] }`; denied/missing state returns literal `null` and the non-disclosing campaign 404. Authorized empty does not suppress the independent campaign-attributable integrity aggregate: orphan sheets, actors, private state, classes, attributes, proficiencies, and choices fail loudly, including descendants retained in the requested campaign after a campaign-character root moves elsewhere. Evidence attributed only to another campaign does not poison the requested campaign. Nonempty results are limited to 1,000 with MAX+1 overflow detection, ordered by campaign-character `created_at` then binary ID, and expose exactly `{ id, characterId, name }`. Names reuse the bounded well-formed UTF-16 legacy projection. Aggregate ancestry is validated through evidence only; controller identity, private notes, sheet/actor fields, commands, and unrelated persona fields are never projected. The route strips internal path-binding evidence and strict-parses the public envelope. Repository/open failures, wrong-path output, malformed summaries, and every non-null falsey output are request-correlated redacted 500s. Scoped campaign failure logs retain only generic operation, method, and route-template context; they never serialize caught exceptions, stacks, SQL/schema/output details, or private field names/values, including repeated use of a cached open error. HEAD and all non-GET methods are absent; no campaign configuration or starter is required.

`POST /rpg/v1/campaigns/:campaignId/characters` is the feature-gated trusted-local original-starter creation operation. It accepts one strict 1–128 character campaign path ID, no raw query delimiter (including bare `?`), exact `application/json` with only an optional charset, and exactly `{ "characterId": string }`. Campaign identity is path-owned; controller `local-owner`, exact starter content, classes, empty arrays, null private notes, sheets, and actor state are server-owned. Valid requests invoke `createOriginalStarterCharacterCreationService` once; its preflight and specialized atomic repository write each run at most once. Success strict-parses and binds the returned `characterId` to the request, returns `201 { "character": { "id", "characterId", "name" } }`, and emits no `Location`.

Slice 88 further defines the roster operation's actual authorization/integrity behavior: once an intact owner, GM, player, or observer membership authorizes, the same statement requires exact `campaigns.owner_role = 'owner'`, exactly one owner-role membership, exactly one owner membership matching the campaign pointer, an intact owner principal, and a strict owner membership row. Malformed, duplicate, mismatched, or orphaned owner state is therefore a request-correlated redacted `RPG_INTERNAL_ERROR` 500 for an authorized actor. A stale purported owner, an actor with a missing authorization parent, and an outsider remain masked by the non-disclosing campaign 404.

The roster paragraph's historical statement that all non-GET methods are absent is superseded only for this exact POST. HEAD and every method other than the documented GET and POST remain absent.

Feature denial precedes all validation. When enabled, query rejection precedes strict path, then media, then one generic malformed/empty/schema-invalid `RPG_INVALID_REQUEST` 400; no repository is opened before all are valid. Exact typed unavailable maps to non-disclosing `RPG_CAMPAIGN_NOT_FOUND` 404, exact persona unavailable to stable `RPG_CAMPAIGN_CHARACTER_PERSONA_NOT_FOUND` 404, and exact duplicate/conflict to redacted `RPG_CAMPAIGN_CHARACTER_CREATE_CONFLICT` 409. Same-code lookalikes, corruption, dependency/SQL/repository/open failures, malformed or falsey output, and request/output mismatches are correlated generic `RPG_INTERNAL_ERROR` 500 responses. Its exact detail is outcome-neutral: `Campaign character creation status is unknown; reconciliation with authoritative character list and creation options GETs is required; never retry automatically`. It exposes no cause or campaign/persona ID and does not claim rollback. Logs contain only generic operation/method/route-template context—not exception, body, identifier, header, query, output, or concrete request path data. POST joins method-sensitive malformed/overlong exact `/characters` normalization while GET retains roster semantics and unsupported/lookalike/legacy behavior remains compatible.

There is no idempotency contract, hidden retry, or automatic POST retry. Every unexpected/malformed-output 500 says status is unknown and requires authoritative roster GET plus creation-options GET reconciliation. A write may have committed, so the response never asserts rollback and must never trigger an automatic POST retry.

The same campaign-attributable aggregate also rejects `rpg_actor_resources` whose actor or campaign-character ancestry is missing or belongs to another campaign. This uses existence/count evidence only: resource data is neither selected nor projected, outsider and stale-owner masking remains first, and corruption attributed only to campaign B does not affect a clean campaign A.

Slice 88 changes no schema or HTTP operation count. Its first-review remediation changes the existing roster's actual authorization/integrity behavior: after any intact owner/GM/player/observer membership authorizes, malformed, duplicate, mismatched, or orphaned canonical-owner state is now loud through the existing redacted 500 path rather than passing the roster projection. Stale purported owners, actors with missing authorization parents, and outsiders remain masked as 404. Client follow-up remediation changes only request ordering/state ownership: post-setup options wait for a current exact-configured detail success, pending pre-setup options cannot settle over setup state, and campaign-scoped refresh intent survives failed reconciliation and detail Retry.

The second independent server review, second independent client review, and contracts/closeout review all reported no findings after all Slice 88 remediation. Slice 89 then ran the mandated exact gate in order: `npm run typecheck` passed; `npm run build` passed with 130 Vite modules; `npm test` passed with contracts 104 across 7 files, server 1,521 passed plus 1 skipped across 67 files, and client 145 across 6 files; `npm run test:e2e` passed 1. Unit total is 1,770 passed plus 1 skipped; deterministic total is 1,771 passed plus 1 skipped. Live E2E was explicitly excluded and not run. Older gate ledgers are historical. This synchronization changed documentation only; no code/tests changed, no verification command was run for the edit, no file was added, and no commit was created. No next implementation slice is approved; future work requires explicit separate scope.

Setup has dual authority: fixed `local-owner` must be the sole canonical local application owner and the campaign pointer's sole canonical owner membership/principal. Inspection validates attributable campaign data before raw configuration identity, allowing exact configured starter namespace failures to become stable conflicts without hiding unrelated corruption. One preflight snapshot and both specialized immediate write transactions validate the full authority graph, campaign setup state, reserved exact profile, reserved pack across all versions, and complete/captured definition namespace before the first write. Exact manifest installation runs first, exact configuration second, then authoritative detail proof. This is convergent, not atomic: an install may remain after a valid first commit. Missing, extra, malformed, captured, wrong-version, unsealed, or incomplete reserved state conflicts without overwrite or repair. There is no hidden retry or automatic startup. The client exposes setup only to an exact owner, requires confirmation, binds success to owner/exact content, and uses GET—not an automatic PUT—to reconcile ambiguity. In-app back navigation is disabled while a mutation is in memory and reload receives a warning, but browsers cannot guarantee reload cancellation; a completed full reload remains ambiguous and must not be treated as permission to retry.

All 21 current RPG operations delegate with the fixed literal trusted-local principal `local-owner`; authorization and user identity headers are ignored. This is unauthenticated single-user local convenience, **not authentication or a remote-safe security boundary**. Builder and progression lanes require campaign plus mechanics flags; administration requires campaign. Feature denial precedes validation. Routes share one lazy app-owned repository and cache either open success or failure for the plugin lifetime; a ready repository closes exactly once.

Schema v15 and the public repository barrel now provide campaign lifecycle/settings, audited membership and room administration, checkpoint/fork, recap, role-safe log/receipt reads, and bounded import/export operations. Transfer packages carry strict public gameplay events and portable actor mechanics while omitting private actor state, idempotency keys, credentials, local paths, and usage history. The narrow administration GET/PATCH route is current; the remaining administration repository operations have no HTTP route. The current boundary is 21 trusted-local operations and future routes remain scoped by ROADMAP M2.1–M2.4.

Schema v18 revision 1 and `contentCatalogRepo.ts` provide the repaired M1.2 repository boundary. They do **not** register generic `/content-packs`, validation, publication, or arbitrary campaign-content HTTP routes; those remain future M2.5 work. The one current exception is the bounded fixed mechanics-starter activation operation, which reuses these repository methods without exposing caller-selected catalog data. The factory repository purely validates strict `validated-v1` publications. Application-owner publication requires an explicit `idempotencyKey` recorded in an immutable submission receipt; exact retries return the original publication without clock/ID use or writes, changed reuse conflicts, and sealed versions remain immutable. Campaign configuration is restricted to the canonical sole owner, requires `expectedRevision` and `idempotencyKey`, advances `administration_revision` exactly once, and atomically appends a catalog command/event/receipt. Exact retries return the authoritative receipt, changed reuse conflicts, stale requests reject, and compatible later revisions may replace current selection without mutating publication history. There is no automatic retry.

Schema v22 revision 1 completes the repaired M1.3 repository and shared-contract boundary over additive v19-v21 draft storage. The current server boundary exposes only draft POST/GET/PATCH; finalization, abandon, receipt, sheet, and physical campaign-deletion routes remain absent and future route shapes remain M2.6 work. Factory callers can create, read, update, abandon, finalize, and read immutable receipts for strict durable or seven-day expiring drafts. Mutations require idempotency identities and revisions where applicable, reject repository-transaction nesting, and are authorized only to a canonical owner, GM, or the draft's eligible player controller. The PATCH adapter binds the draft to the campaign with an authoritative read before mutation. Reads return no private notes or persona profile fields; unrelated players, observers, outsiders, and missing drafts receive no draft projection.

Drafts snapshot the exact current `validated-v1` rules profile, ordered pack versions, and publication digests. They expose four fixed required groups—race, background, one level-one class, and starter kit-or-currency—plus deterministic completion issues and a server-derived preview. Allocation is exactly standard array, exact-budget point buy, bounded manual scores, or server roll. A server roll accepts no result from its caller, requests each of 24 physical d6 values independently from injected RNG range `[1,7)`, forms six canonical 4d6/drop-first-lowest terms using `velvet-4d6-drop-first-lowest-v1`, and persists every die; an exact committed retry returns the original receipt without RNG, clock, ID, or write replay. Expiry is effective at read/command time and never silently rewrites a draft.

Schema v20 binds every draft command to an immutable exact proposed event ID/type/canonical event and canonical result. Additive v21 permits exactly one command, proposal, event, receipt, and matching immutable result snapshot per resulting revision, including create revision zero. Additive v22 makes the old marker unconditionally inert and guards campaign plus every builder child against physical deletion; registering the legacy UDF name grants nothing. No permanent-delete repository method or deletion UDF exists. `request_digest` is informational and retry identity remains canonical `requested_json`. Migration and every current startup validate complete audit/provenance, root-state agreement, and exact equality between `created_by_principal_id` and the immutable create-command actor. Fixed canonical full-SQL attestations reject changed constraints/FKs, same-name no-op triggers, and missing/extra artifacts. Product lifecycle uses archive, and linked personas continue to return `in-use`.

The pure `velvet-character-derived-v1` calculator is the sole authority for max HP, guard/evasion/will defenses, initiative, speed, carrying limit, spell attack, and save DC. Results include fixed formula/input explanations and accept no client override. Finalization revalidates authority, persona/controller eligibility, exact unchanged pins, allocation, level-one definitions, and every fixed choice under one `BEGIN IMMEDIATE`; it then materializes the existing campaign character, sheet, one class, final attributes, proficiencies, resolved choices, actor, null-notes private controller state, and initial health. The same commit stores an immutable derived snapshot, exact starting kit-or-currency grant ledger, one draft revision, and immutable command/event/receipt. M1.5 materializes its repository-owned inventory/wallet state without adding any HTTP route or client/UI. Incomplete, expired, abandoned, stale, pin-drifted, or failed drafts create no playable row, and every failure rolls back the whole attempt. Persona editing remains the separate existing `/characters/:id` concern.

Owner/GM/player/observer catalog projections are structurally distinct. Public roots and closure are persisted at seal time: entire cyclic or acyclic subgraphs reachable only through enemy-private references are absent unless independently reachable from a public root. Player/observer reads attest every public sidecar row and aggregate digest before parsing only reachable values, so private-only names, mechanics, and poisoned values never enter the projection; observers additionally receive presentation metadata only. Campaign-scoped legacy definition reads perform that same row and aggregate verification before consulting reachability. Corrupt validated sidecars fail loudly for owner/GM legacy reads and produce no definition for player/observer legacy reads. Authorized modern reads validate publication identity, profile, canonical digest, report, counts, public attestation, definitions, v10 metadata, exact pins, and complete audit; outsiders are masked first. Publication accepts only the closed `velvet-starter-v1` vocabulary and rejects paths, files, URLs, scripts, formulas, executables, multiclass execution, and open-ended effect DSLs. Each validated publication also writes all seven v10 metadata kinds (`enemy-template` maps to `enemy`). Migrated sealed packs remain honest `legacy-v10` without fabricated validation and cannot be selected by the validated operation.

Catalog events and receipts participate in the same contiguous campaign administration revision history as M1.1 events. SQL rejects cross-family command-identity or revision overlap and closes a catalog command only when its actor, target digest, non-empty exact ordered authoritative pins and publication digests, proposed event identity, byte-identical public payload, and exact receipt result agree. Administration list/receipt reads, export, dry-run, import, and apply merge or transfer both immutable families in exact revision order. Schema v17 remains additive over the original v16 sidecars and derives visibility exclusively from strict full definitions rather than trusting caller-stored public JSON. Schema v18 additively seals exact command proposals. Both migrations enumerate their canonical prior tables, columns, indexes, and triggers before writing; incompatible markers reject transactionally.

The separate built-in `velvet:mechanics-starter` catalog is original clean-room material, recursively frozen, digest-versioned, and installed/configured only through explicit repository methods. It is distinct from and does not modify the existing metadata-only `velvet:original-starter` or its current HTTP setup route. No catalog is installed automatically at startup.

Schema v23 introduced the M1.4 single-class progression repository/shared contracts, and v24r1 repaired its provenance/integrity graph without rewriting historical ledgers. Schema v25r1 completes M1.5 as factory-only repository/shared-contract behavior with exact retries, revisions, and immutable receipts. It adds no HTTP route or client/UI; the current 21-operation boundary is unchanged. The fixed canonical v25 DDL digest is `a5e3a58f8014978315d20440a0ac087871edac95323d059327faa2fe0a983ef7`. Builder and progression server routes remain integrated; progression remains read/preview-only and no client workflow exists.

Unknown well-formed routes under `/rpg/v1` return structured problems. Pre-routing malformed or overlong IDs are normalized only for reviewed exact campaign shapes, now including both workspace parameters. Feature denial remains first; supported methods return route-appropriate query 400 before path 404, while unsupported methods remain absent. Problem instances retain safe templates only. Malformed unknown, lookalike, and legacy paths retain raw JSON/code/status compatibility and the effective 128-character parameter cap: 101–128 character legacy values still reach existing handlers and 129 remains the same raw 414. A larger internal router allowance exists only so exact nested workspace shapes can strictly normalize either ID before an early compatibility hook restores every other cap response.

Provider settings accept `pricing.promptPerMillion` and `pricing.completionPerMillion` in USD. `/usage` returns append-only lifetime prompt/completion totals and calls, provider-reported versus locally estimated token counts, and operation/model/session breakdowns. Successful provider-backed character generations are measured or estimated; local stubs, safe-word acknowledgements, and failed requests are absent. Auxiliary routing/synthesis calls are recorded when their provider response includes usage. `estimatedCostUsd` is `null` until both prices are configured; otherwise the current rates are applied to all ledger entries as an estimate, not a provider invoice or historical per-model rate card. Usage survives session deletion.

### Campaign-character workspace

Workspace GET is actual HTTP `Cache-Control: no-store` behavior on success and scoped errors, including exact malformed-path normalization. The browser client also requests this operation with `cache: "no-store"`.

`GET /rpg/v1/campaigns/:campaignId/characters/:campaignCharacterId/workspace` accepts two strict 1–128 character resource IDs and no raw query delimiter, including a bare `?`, and calls only `getCampaignCharacterWorkspace("local-owner", campaignId, campaignCharacterId)`. One explicit-column statement provides a factory statement snapshot or the enclosing UoW snapshot. Parent-backed owner/GM/player/observer access is preserved and exact owner integrity is checked after authorization. The statement also validates campaign-attributable ancestry evidence independently of the selected root: detached or mismatched sheets, actors, private state, classes, attributes, proficiencies, choices, and actor resources are loud even when one valid selected root remains. This evidence includes no private payload, and evidence attributable only to another campaign cannot poison the requested campaign. Selected race/background/class/choice definitions require exact selected-profile sealed pins and valid definition metadata; all collections and resources use MAX+1 bounds and reject malformed raw types, positions, and identities. Stale purported owners, outsiders, missing state, and cross-campaign targets return literal `null` and the same non-disclosing `RPG_CAMPAIGN_CHARACTER_NOT_FOUND` 404. Authorized attributable corruption is loud and becomes a neutral redacted 500.

The internal snapshot path-binds both IDs, while the strict public response contains only character display name; resolved race/background names and descriptions; ordered class metadata and levels; positional attributes, category-specific proficiencies, choices, and resources. It validates persona/sheet/actor one-to-one and campaign ancestry, timestamps/raw SQLite values, exact sealed pins and race/background/class/choice definitions, duplicates, MAX+1 bounds, and resource ancestry. Arbitrary technical IDs are validated but never humanized or exposed; labels are only exact one-based contract forms. Controller/private notes, persona boundaries/safe word, audit, command, and unrelated persona fields are absent from SQL payload/output, so poisoned private payloads are ignored. Every non-null falsey, malformed/path-mismatched output and open/repository/corruption failure is a request-correlated redacted 500. Feature denial is first, query rejection precedes strict path handling, malformed either-ID exact routes are normalized without widening lookalikes or legacy behavior, HEAD/unsupported methods are absent, and the repository cache is shared.

### Campaign dice

`GET /rpg/v1/campaigns/:campaignId/dice-rolls` and `POST` to the same route require both campaign and mechanics flags, fixed trusted-local `local-owner`, one strict campaign ID, and no raw query delimiter. GET returns exactly `{ characters, rolls }`: characters are contiguous one-based `{ position, name }` entries and rolls are at most 20 newest-first `{ character, occurredAt, result }` entries bound exactly to that list. Newest order is the array's descending revision/event identity order, not timestamp order; canonical `occurredAt` is informational and may regress or equal another roll's timestamp. Duplicate names remain distinguished by position. One explicit bounded statement chooses the latest 20 dice identities before the term join (at most 2,000 valid rows), while complete timeline revisions, all event variants, outside-window corruption, and attributable audit orphans remain validated. Only owner/GM authority is exposed; player, observer, missing, denied, and stale-owner state share non-disclosing campaign 404 behavior.

POST requires exact JSON `{ "character": { "position": integer, "name": string }, "expression": DiceExpression }`. One preflight UoW snapshot resolves exact current position/name, campaign-character and actor ancestry, active timeline, and revision, then closes before ID generation or execution. A separately injected server generator creates one internal value used as command ID and idempotency key. The service constructs exact `roll_actor_dice` with `sourceTurnId: null` and invokes one factory-only executor once without retry. Under one `BEGIN IMMEDIATE`, that executor revalidates current `(created_at,id)` roster ordering, exact safe position/name, campaign-character identity, and actor ancestry before RNG, event ID, clock, or writes, then uses the unchanged generic atomic command path. Rename/insertion/deletion drift is typed 409; malformed persistence remains an internal error. The service validates receipt/event/result and returns `201 { "roll": { "character", "occurredAt", "result" } }` without `Location`. Other failures are neutral commit-ambiguous 500s requiring history refresh and no automatic retry. Responses are `no-store`; IDs/revisions, checks, DCs, narration, raw commands, and private state are excluded. HEAD and methods other than GET/POST remain absent.

The Slice 94 client accepts only that strict request-bound 201 as commit confirmation. It reports latest-history refresh separately and never identifies a particular identity-free entry as that roll or requires its presence. Network, 500, malformed 2xx/body, and other untyped outcomes remain ambiguous regardless of identical history projections. Exact `RPG_CAMPAIGN_DICE_BINDING_CONFLICT` 409 and `RPG_CAMPAIGN_NOT_FOUND` 404 are consumed as known non-commits because the documented service ordering classifies them before execution. Every issued POST still owns one fresh GET, and persistent status offers only a separate GET-only Refresh rolls action. Rapid manual Retry or Refresh pointer/key activation is synchronously limited to one GET until settlement without affecting reconciliation. This is client consumption of the existing statuses and does not change any HTTP semantics.

## Characters

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/characters` | List characters |
| `POST` | `/characters` | Create a character |
| `GET` | `/characters/:id` | Read a character |
| `PATCH` | `/characters/:id` | Update a character profile |
| `DELETE` | `/characters/:id` | Delete an unused character |
| `GET` | `/characters/:id/export` | Export `velvet-character@1` JSON |
| `POST` | `/characters/import` | Import and revalidate character JSON |

A character deletion returns `409` while any session or campaign character references the character. Remove those references first.

## Memories

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/characters/:id/memories` | List active, pending, and forgotten memories |
| `POST` | `/characters/:id/memories` | Create a manual memory |
| `PATCH` | `/memories/:id` | Edit `content`, `kind`, approval, or restore with `forgottenAt: null` |
| `POST` | `/memories/:id/restore` | Restore a forgotten memory |
| `DELETE` | `/memories/:id` | Soft-forget a memory |

Memory kinds are `fact`, `preference`, and `event`. Manual memories default to approved. Concrete commands such as `remember that I prefer tea` extract up to three immediately approved memories because the command itself is explicit user approval. Natural disclosures such as `my name is...` or `I prefer...` become pending memories for review. In room turns, requests such as `remember your characters` select the whole cast up to the room cap and create an approved snapshot from each selected character's recent self-established details instead of storing the instruction text. Duplicate active memory content is ignored. Only approved, non-forgotten memories are included in prompts.

## Lore

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/lore?characterId=:id` | List all lore or global plus matching character lore |
| `POST` | `/lore` | Create lore |
| `PATCH` | `/lore/:id` | Edit content, keys, scopes, enabled state, or order |
| `DELETE` | `/lore/:id` | Delete lore |

Use `characterIds: []` for global lore or provide one or more character IDs for scoped lore. The legacy singular `characterId` field remains accepted. Enabled entries with no keys are always active; keyed entries activate when recent conversation text contains a key. Prompt selection is ordered by `insertionOrder`, capped at six entries, and constrained by the harness lore budget.

## Sessions

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/sessions?characterId=:id` | List all sessions or filter by participant |
| `POST` | `/sessions` | Create a single- or multi-character session |
| `POST` | `/sessions/solo` | Open or reuse a durable open one-character session |
| `GET` | `/sessions/:id` | Read a session and its active-branch messages |
| `GET` | `/sessions/:id/context` | Read editable authoritative and computed shared context |
| `PUT` | `/sessions/:id/context` | Update the session's authoritative current scene |
| `DELETE` | `/sessions/:id` | Permanently delete session history |
| `GET` | `/sessions/:id/messages` | Read active-branch messages |
| `POST` | `/sessions/:id/messages` | Add a user turn and generate one reply |
| `POST` | `/sessions/:id/stream` | Stream a user turn and reply over SSE |
| `POST` | `/sessions/:id/room-turn` | Route a message to pertinent participants and generate bounded sequential replies |
| `POST` | `/sessions/:id/room-continue` | Generate another bounded room exchange without a user message |
| `POST` | `/sessions/:id/continue` | Generate one character turn without a user message |
| `POST` | `/sessions/:id/stop` | Close a session and abort active generation |
| `POST` | `/sessions/:id/generation/cancel` | Cancel a matching streamed generation |

Create a legacy single-character session with:

```json
{ "characterId": "character-id", "title": "Optional title" }
```

Create a group session with:

```json
{
  "characterIds": ["aria-id", "noor-id"],
  "primaryCharacterId": "aria-id",
  "title": "Station evening",
  "presetId": "default"
}
```

Sessions support 1-12 unique participants. `characterId` remains in responses as a backward-compatible alias for `primaryCharacterId`; `participants` contains the hydrated character profiles. Participants cannot currently be changed after creation.

`POST /sessions/solo` accepts `{ "characterId": "..." }` and returns `{ session, messages, created }`. It reuses the newest open exact-solo session for that character or creates one. Group and closed sessions never qualify. This isolates conversation history but does not provide authorization.

Turn bodies accept optional `speakerCharacterId`; it must identify a session participant and defaults to the primary character. Character messages persist that identity as `speakerCharacterId`.

`POST /sessions/:id/continue` accepts `{ "speakerCharacterId": "..." }` and performs exactly one buffered character generation. With a configured provider, completion is followed by at most one bounded scene-synthesis request, for a ceiling of two provider calls.

`POST /sessions/:id/room-turn` accepts `{ "content": "...", "maxSpeakers": 4 }`. `maxSpeakers` is optional, defaults to 3, and is constrained to 1-6. With a usable provider, the server performs one routing request, validates the returned participant IDs, and generates one reply for each selected participant in order. Without one, routing uses the deterministic fallback without a provider call. Broad group language such as `everyone`, `guys`, `gang`, `both`, or `you two` expands selection when the cap permits. The first speaker initiates toward relevant selected participants; each later speaker receives the preceding character's exact reply and responds directly. Each selected participant replies at most once per round.

`POST /sessions/:id/room-continue` accepts optional `{ "maxSpeakers": 2 }`, constrained to 1-6. It requires at least two participants and a preceding character reply. No user message is created. Routing chooses pertinent speakers, and the server moves a participant other than the latest speaker to the first position when possible. Each generated reply directly receives and answers the preceding character's exact content. The call stops after the selected replies, uses the normal per-session generation lock, and returns `replies`, `selectedSpeakerIds`, `routing`, `messages`, and session state. The client implements autoplay by invoking this bounded endpoint sequentially for at most three configured rounds.

After each successfully completed room turn or continuation, the server performs at most one scene-synthesis request. A configured-provider room round therefore costs at most `1 routing + maxSpeakers generations + 1 synthesis`: eight calls at six responders. An initial round plus three autoplay rounds is capped at 32 calls.

Both room endpoints retain their JSON response by default. Send `Accept: text/event-stream` for progressive delivery. Room-turn event order is `user_message` → `state` → `room_reply`+ → `room_done`; room continuation starts at `state`. Each `room_reply` contains `{ reply, index, total }` and is emitted only after that attributed reply is persisted. `room_done` contains the complete JSON result for reconciliation.

## Prompt Templates

`GET /prompt-templates` returns 20 ordered template definitions covering character safety/persona/constraints/custom style/lore/memory/shared-context/final instructions, provider start-reply behavior, room routing, first/follow-up room speakers, single/room continuation, and factual scene synthesis. Each entry includes `id`, `label`, `description`, `defaultTemplate`, effective `template`, allowed `placeholders`, and `overridden`.

`PUT /prompt-templates/:id` accepts `{ "template": "..." }`. Unknown placeholders and templates over 64,000 characters are rejected. Send `{ "template": null }` to reset that layer to its application default. Overrides persist inside the harness settings JSON and require no schema migration. API keys are never included.

## Shared Context

`GET /sessions/:id/context` returns `{ context }` with combined authoritative `sourceOfTruth`, editable manual `editableSource`, model-produced `synthesizedSource`, their update timestamps, session state, participant names/archetypes, recent events, approved memories, triggered lore, and unresolved questions/goals. The synthesized state contains categorized current facts rather than copied chat lines. It is rewritten after completed turns from manual canon, its prior state, and recent active-branch messages. The same authoritative text is injected into every buffered and streamed character generation.

`PUT /sessions/:id/context` accepts `{ "sourceOfTruth": "..." }` as editable manual canon (up to 8,000 characters after trimming) and returns `{ source: { sourceOfTruth, updatedAt } }`. A later `GET /sessions/:id/context` combines manual canon with synthesized scene facts; manual canon has highest priority.

## Branches and Swipes

| Method | Route | Purpose |
| --- | --- | --- |
| `GET` | `/sessions/:id/messages/:mid/siblings` | List branch siblings and active selection |
| `POST` | `/sessions/:id/messages/:mid/activate` | Activate a message branch |
| `POST` | `/sessions/:id/messages/:mid/swipe` | Generate a buffered alternative reply |
| `POST` | `/sessions/:id/messages/:mid/swipe/stream` | Stream an alternative reply |
| `POST` | `/sessions/:id/branch` | Retry or edit a user turn on a new branch |

Swipes require a character reply directly parented by a user message. They inherit the original speaker unless another participant is supplied. Continuation replies cannot be swiped. Branch activation refreshes the active-branch summary and, when a usable provider is configured, synthesized scene state.

See [streaming.md](streaming.md) for SSE events and cancellation behavior.

## End-to-End Contract Coverage

`npm run test:e2e` exercises these routes through a running Vite proxy and Fastify process with isolated temporary data. Its current inventory includes deterministic campaign creation/open/rename, workspace presentation, starter setup and character creation, exact deterministic dice output and reload, campaign-room attach/open/return/stopped/conflict flows, and a real committed room PUT whose browser response is aborted to prove exactly one PUT plus one reconciliation GET and no retry. It also covers public list verification, provider redaction, CRUD persistence, buffered/streamed/room turns, speaker attribution and chaining, continuation, prompt-template override/reset, combined manual/synthesized context, memory and lore lifecycle operations, safe-word closure, closed-session rejection, and deletion cascades. Opaque special-character session path encoding remains unit/integration coverage because normal server ID generation cannot practically create such IDs in this real E2E workflow.

`VELVET_E2E_LIVE=1 npm run test:e2e:live` uses a temporary online backup of the configured application database. It verifies the configured provider through `GET /provider`, which must include `hasApiKey` and must not include `apiKey`, then exercises buffered and streamed turns, a two-speaker room turn, room continuation, synthesized factual context, and usage growth including `character_reply` and `scene_synthesis`. The workflow is capped at 12 provider calls. Tests use unique record names and remove only records they created in the cloned database.

## Internal implementation map (non-contractual)

`server/src/app.ts` owns Fastify construction, global request behavior, and plugin registration; route handlers are not implemented there. Legacy read/management handlers are split across focused plugins under `server/src/routes/roleplay/`. `server/src/routes/roleplay/interactions.ts` owns message, room-turn/continuation, streaming, cancellation, swipe, activation, branch, and single-continuation handlers. It delegates common buffered/streamed generation, scene synthesis, and summary updates to `generationService.ts`, while `generationRegistry.ts` owns process-local per-session exclusion and active-stream abort state shared with `sessionLifecycle.ts`.

RPG HTTP handlers remain under `server/src/routes/rpg/v1/`. All persistence consumers import the public `server/src/repo/index.ts` barrel; schema/migration and repository-domain ownership are documented in [repo-architecture.md](repo-architecture.md). These locations describe the current implementation only. The routes, methods, request/response shapes, status handling, and other contracts above are unchanged by this map.
